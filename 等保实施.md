# 实施记录

文档大题结构：

- **〇、开局配置（最前）：** 做具体业务页之前先搭好的公共能力——带 token、认人、统一响应、**写入后 refresh 拿 id** 等；**写一次，后面每个要「当前用户」的接口只挂上复用**  
- **一：** **登录**（契约 → 后端 → 登录页 → 浓缩；原一～四已合并）  
- **五：** 项目列表到差距状态机（5.1～5.10；①～⑦ 是状态联调步骤号）  
- **六：** 身份与角色管控（把开局认人接到 ACTION_ROLES；6.1～6.7）  
- **七：** 数据范围 / 项目成员过滤（project_member；经理可看全部；7.1～7.8）  
- **八：** Layout 嵌套路由 + 侧栏导航  
- **九：** **开通账号（注册收口）**——挂在 Layout 侧栏，经理在系统里建用户；不是登录页自助注册  
- **十：** 第一版总复习清单  
- **十一：** 新建项目 / 分配与移除成员（含提问整理与踩坑；第一版管理侧补强）  
- **十二：** 前端方案 A 工作台美化（侧栏 + 顶栏 + 白卡片）  
- **十三：** 第一版收口落地（退出 / 录入 / 路由守卫 / 401 跳转）+ 登录回车与拦截器答疑  
- **十四：** 开通账号类型与校验（TS interface + el-form rules + 后端 Pydantic Field；含 FormRules 答疑）  
- **十五：** Agent 查业务库 + 短期记忆 + 结构化输出（对照尚硅谷课 Tools / Memory / Structured；**不含 RAG**）  
- **十六：** 三四天整体复习地图（业务 + Redis + Agent）

**第一版范围（可整本复习）：** 开局配置 → **登录** → 项目/差距/抽屉/状态机 → 角色 → 成员过滤 → Layout → **经理开通账号（九）** → 新建项目 / 加人 / 删人（十一） → 工作台 UI（十二）→ **收口（十三）** → 开通校验（十四）。  

**已提前落地（原标「第二版」）：**  
- Redis 三块（token / 项目列表 / 差距统计）→ 详见根目录 **`缓存点.md`**  
- Agent 对话三句查库 + 短期记忆 + 结构化 → 见 **十五**  

**仍后置 / 可不做：** 向量库 RAG；缓存击穿/雪崩/分布式锁；开通账号角色下拉；Agent 长期记忆 Store。  

**怎么读：** 先看 **〇**；再按一→十四；卡 Redis 看 **`缓存点.md`**；卡 Agent 看 **十五**；临答辩按 **十六** 排期复习。  
**本地刷题：** 根目录 **`高频问答.md`**（本地忽略、不入库；按模块 ★ 优先级对照本笔记答题）。

---

## 〇、开局配置（做业务功能之前先写好）

> **这一章放最前的原因：**  
> 登录、改状态、开通账号、新建项目 / 加人，全都要「知道当前是谁」。  
> 认人、带证、统一成功/失败——属于**公共模板**：函数只写一次，后面每个新接口挂上即可。

开局配置要**按前后端分层记**，不要揉成一张大表。

```text
前端开局：axios + 拦截器 + Vite 代理 + App/main（壳）+ Element Plus
后端开局：get_db、统一成功/失败、CORS、get_current_user（认人）
两边对上：登录把 token 存 localStorage → 拦截器带出去 → 后端 Depends 认人
```

---

### 〇.1 总览（先分清前后端）

#### 前端开局配置

| 配置 | 文件位置 | 一句话 |
|------|----------|--------|
| axios 请求实例 | `front/.../utils/request.js` | 所有业务 API 都走这一个 `request` |
| 请求拦截器 | 同上，挂在实例上 | **在请求发出前**统一加工（本项目：自动加 token） |
| 响应拦截器 | 同上 | **收到错误响应后**统一处理（本项目：HTTP 401 → 清证 → `/login`；见 **〇.2 / 十三**） |
| 开发代理 | `vite.config.js` → `proxy` | 前端写 `/api/...`，开发时转到后端 `8000` |
| 根组件壳 | `App.vue` | 只留 `<router-view />`，页面由路由换 |
| 应用入口 | `main.js` | `createApp` + Element Plus + router，挂一次 |
| 路由表入口 | `router/index.js` | 开局先有 router 实例；具体 path 随业务加 |

#### 后端开局配置

| 配置 | 文件位置 | 一句话 |
|------|----------|--------|
| 数据库会话 | `config/request.py` → `get_db` | 路由/Depends 里拿 `AsyncSession` |
| 成功返回壳 | `utils/response.py` → `success_response` | 统一 `{ code, message, data }` |
| 失败返回壳 | `utils/exception.py` | `HTTPException` 也变成统一 JSON |
| CORS | `main.py` | 浏览器跨域能调后端 |
| token → 当前用户 | `utils/auth.py` → `get_current_user` | 从 Header 认人，得到 `User` |
| token 查库 | `crud/user.py` → `get_user_by_token` | 被上一层调用：纯 token → User |
| 写入后拿自增 id | crud 习惯：`add` → `commit` → `refresh` | 新建项目后再写成员表等（见 〇.5） |

---

### 〇.2 前端开局（详细）

#### 1）先有 axios 实例

业务页不直接 `axios.get`，而是 `import request from '@/utils/request'`，保证超时、baseURL、拦截器只配一份。

#### 2）再挂「请求拦截器」——先理解它是什么

**拦截器 = 每个请求发出去之前，自动跑的一段公共代码。**  
业务页只管 `request.post('/api/...')`，不用每个函数自己写 Header。

本项目拦截器里做的事（细节）：

1. 从 `localStorage` 读 `token`（登录成功时存进去的）  
2. 若有 token，写到请求头：`Authorization: Bearer <token>`  
3. 把改好的 `config` 返回，请求继续发出  

```js
// front/.../utils/request.js
instance.interceptors.request.use(function (config) {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})
```

**以后凡是要登录才能调的接口**（项目列表、action、开通账号、新建项目、加人……）都自动带证，前端不必改来改去。

**和登录的分工：** 登录页负责「拿到 token 并 `setItem`」；拦截器负责「以后每次请求带上」。没有登录存 token，拦截器加了也是空的。

#### 2b）再挂「响应拦截器」——401 统一踢回登录（收口后补上）

**响应拦截器 = 每个请求回来之后（尤其是失败时）自动跑的公共代码。**  
业务页不用每个 `catch` 里自己写「401 去登录」。

本项目做法（细节与踩坑见 **十三.3**）：

```js
instance.interceptors.response.use(
  function (response) { return response },
  function (error) {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      localStorage.removeItem('userInfo')
      location.href = '/login'
    }
    return Promise.reject(error)
  },
)
```

**和路由守卫的分工：**  
- 守卫（`beforeEach`）：进**页面**前看有没有 token 字符串。  
- 响应拦截器：页面里用 `request` 调接口，后端回 **HTTP 401**（token 过期/无效）时清证并跳登录。  
二者缺一不可；详见 **十三.2～十三.3**。

#### 3）Vite 开发代理（只配一次）

文件：`front/front_mini_agent/vite.config.js`

浏览器打的是前端端口（如 5173），后端在 8000。开发时用代理：前端代码只写 `/api/...`，Vite 转到后端，少踩跨域。

```js
proxy: {
  '/api': {
    target: 'http://127.0.0.1:8000',
    changeOrigin: true,
  },
}
```

**注意：** 这是**开发期**配置；和后端 `main.py` 里的 CORS 常一起准备。业务页（登录、项目……）不再重复写代理。

#### 4）Vue 应用壳（只挂一次）

`App.vue` 不做业务，只留出口：

```vue
<!-- App.vue -->
<template>
  <router-view />
</template>
```

`main.js` 挂插件（Element Plus、router）一次即可：

```js
// main.js
createApp(App).use(ElementPlus).use(router).mount('#app')
```

**坑：依赖装在外层 `front/`，真正项目在 `front_mini_agent/`**  
`element-plus` / `axios` / `vue-router` 要在 `front_mini_agent` 下 `pnpm add`。

`router/index.js` 开局先有实例和空/最小 routes；**登录、Layout 等 path 在对应章节往里加**，不要每章重写 `createApp` / `App.vue`。

> **纪律：** 〇 写过的公共能力（代理、壳、拦截器、成功壳、CORS、认人），后面章节**只引用、不重贴整段代码**。业务章只写本页新增的接口/路由/组件。

---

### 〇.3 后端开局（详细）

#### 1）`get_db` / 成功壳 / 失败壳 / CORS

| 项 | 作用 | 以后场景 |
|----|------|----------|
| `get_db` | 每个接口拿数据库会话 | 所有 crud |
| `success_response` | 成功格式统一 | 所有返回成功的接口 |
| 异常处理器 | `raise HTTPException` 也统一成 JSON | 401/403/400 前端好解析 |
| CORS | 前端端口调后端端口不被浏览器拦 | 本地 Vue + FastAPI 联调 |

这些开局就要有；业务章（登录、项目……）只 `include_router` / `Depends`，不重写壳。

#### 2）认人主入口：`get_current_user`（最常复用）

**作用：** 读 Header 里的 `Authorization` → 剥掉 `Bearer ` → 调 `get_user_by_token` → 返回 `User`；没有/无效 → **401**。  
路由里一行就能拿到 **`current_user.id` / `current_user.role`**。

**`get_user_by_token`：** 只负责查库（token 在不在、过不过期、对应用户）。一般**不直接在路由里调**，由 `get_current_user` 调用。

**调用顺序：**

```text
路由 Depends(get_current_user)
  → 读 Authorization，剥 Bearer
  → get_user_by_token(db, token)
       → 查 user_token → 查过期 → 查 user
  → 返回 User 或 401
```

**可复用代码（开局写好，后面只 Depends，不改这两份）：**

`back/utils/auth.py` → `get_current_user`：

```python
async def get_current_user(
    authorization: str | None = Header(default=None),
    db: AsyncSession = Depends(get_db),
) -> User:
    if not authorization:
        raise HTTPException(status_code=401, detail="未登录或缺少 token")

    # 前端：Authorization: Bearer <uuid>
    token = authorization
    if authorization.lower().startswith("bearer "):
        token = authorization[7:].strip()

    if not token:
        raise HTTPException(status_code=401, detail="token 无效")

    user = await get_user_by_token(db, token)
    if not user:
        raise HTTPException(status_code=401, detail="登录已失效，请重新登录")
    return user
```

`back/crud/user.py` → `get_user_by_token`：

```python
async def get_user_by_token(db: AsyncSession, token: str):
    stmt = select(UserToken).where(UserToken.token == token)
    row = (await db.execute(stmt)).scalar_one_or_none()
    if not row:
        return None
    if row.expires_at < datetime.now():
        return None
    stmt_user = select(User).where(User.id == row.user_id)
    return (await db.execute(stmt_user)).scalar_one_or_none()
```

**以后需要认人的场景（只挂 Depends，不重写上面两段）：**

| 场景 | 为什么要认人 |
|------|----------------|
| 差距 action / logs | 操作人写入历史；再判角色 |
| 项目 list / 差距 list | 按成员过滤；经理看全部要 role |
| 经理开通账号 | 必须是 manager |
| 新建项目 | 创建人 id 写入 `project_member`，不能信前端自报 |
| 加人 / 删人 | 谁在操作；是否经理 |
| 录入 / 对话查库 | 录入属第一版收口（十三已做）；对话查库第二版 |

**怎么挂：**

```python
from utils.auth import get_current_user
from model.chat import User

@router.post("/create")
async def create_project(
    ...,
    current_user: User = Depends(get_current_user),
):
    # 用 current_user.id / current_user.role
    ...
```

**口诀：** 开局把认人写好 → 每新开一个要登录的接口，只加 `Depends(get_current_user)`。

---

### 〇.4 和后面章节的关系（别混）

```text
〇 开局
  前端：实例 + 拦截器带证
  后端：统一壳 + get_current_user 认人
  → 一：登录，负责「发出 token、存 localStorage」
  → 五：业务状态机
  → 六：认人接到「角色管动作」
  → 七：认人接到「成员管数据范围」
  → 九 / 十一：开通、建项目、加人——继续 Depends
```

**历史说明：** 实际编码时拦截器和 `get_current_user` 是在第六章才接到业务上的；复习提成 **〇**，表示这是开局能力，不是某个业务页私有逻辑。细节步骤仍可看 6.2、6.3。

---

### 〇.5 `add` / `commit` / `refresh`（后端写入习惯）

新建用户、新建项目、再写成员表时都会用到。三步别混：

| 步骤 | 干什么 |
|------|--------|
| `db.add(row)` | 告诉会话：准备插入这行（**还没真正写库**） |
| `await db.commit()` | **真正入库**；自增 `id` 很多环境下这时已填进 `row.id` |
| `await db.refresh(row)` | **再从数据库读一遍**，把库里最新值同步到内存里的 `row`（`id`、默认时间等） |

**一句话：** 入库靠 `commit`；`refresh` 不是入库，是刷新 `row`，保证能稳妥用到库生成的字段。

**写成员表要用 `row.id` 时，习惯顺序：**

```text
Project(...) → db.add → commit → refresh → 用 row.id 建 ProjectMember → add → commit
```

有的环境只 `commit` 后 `row.id` 也有了；加 `refresh` 更稳妥（和 `add_user` 同一套路）。

**以后场景：** 新建项目后插 `project_member`；任何「先插主表、再用新 id 插从表」的 crud。



---

## 一、登录（契约 → 后端 → 登录页）

我的思路：登录的我的思路：整体来说形参是需要用户名和密码的，然后登录的时候根据用户名和密码先判断段用户是否存在，存在是否密码正确。之后如果存在且密码正确的话则可以先返回对应的用户。为什么先要返回对应的用户，是因为我们的登录是根据用户是否具有token登录的，我们需要把token加在对应的用户上进行返回才可以。所以是先返回一个用户作为对象。

总结：先验人拿到用户 → 再按 id 挂 token → 最后一起返回。







> **开通账号不在本章。** 经理在 Layout 侧栏建用户 → **第九章**。

整条线：

```text
先定契约（字段 / 成败）
  → 后端：模型 → schema → router（可占位）→ crud → 挂真逻辑 → /docs 测通
  → 前端：api → 路由 → Login.vue → 存 token → /project
```

做任何页面之前，先写清：功能、字段、成功作用、失败表现、实施顺序。

---

### 1.1 契约

#### 功能

| 功能 | 说明 |
|------|------|
| 登录（先有种子用户） | 账号密码校验，发 token，进系统 |

#### 字段

| 场景 | 字段 | 说明 |
|------|------|------|
| 登录请求 | `username`、`password` | 不要工号 |
| 成功 data | `token`、`userInfo` | `userInfo` 含 id、username、employee_no、role |
| 不返回 | `password` | 密码只在后端比对，不回给前端再对一遍 |

#### 接口

| 方法 | 路径 | 请求体 |
|------|------|--------|
| POST | `/api/user/login` | `{ username, password }` |

#### 成功 / 失败

成功（走 `success_response`）：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "uuid字符串",
    "userInfo": {
      "id": 1,
      "username": "da123",
      "employee_no": "E001",
      "role": "inspector"
    }
  }
}
```

失败（走全局异常处理器，不是中间件）：

```json
{
  "code": 401,
  "message": "用户名或密码错误",
  "data": null
}
```

| 情况 | 状态码 | message |
|------|--------|---------|
| 用户名不存在或密码错 | 401 | 用户名或密码错误（两句合成一句，不暴露是哪一种） |

#### 定下来的判断

- Token ≠ AI 额度。Token 是登录凭证，UUID 写入 `user_token`，7 天过期，必须返回前端。
- 异常处理器 ≠ CORS 中间件。中间件每个请求都过；异常处理器只在 `raise HTTPException` 时触发。
- **登录页只负责登录。** 开通账号见 **第九章**。

---

### 1.2 后端（先测通 `/login`，再写登录页）

```text
契约
  → 表 / 模型
  → schema（loginRequest，定请求体长什么样）
  → router（路径 + 入参；真逻辑先注释，`data="data"`）
  → crud（按注释签名实现）
  → 回到 router：取消注释，换成真 data
  → main 只 `include_router`（成功壳/异常/CORS 见 **〇**，不开第二遍）
  → /docs 测登录
```

**为什么 router 在 crud 前面：**  
先定路径和入参（`user_data`、`db`），流程用注释勾出来；crud 还没写时，先 `return success_response(message=..., data="data")` 这种占位。参数想清了再写 crud，最后取消注释换上真调用。

**造第一个用户：** 库里没人时登录测不了——SQL / 种子插一条即可。`add_user` 等给第九章开通用。

#### 表 / 模型

文件：`back/model/chat.py`

思路：基类放共用时间；登录相关拆两张表——**User** 存用户，**UserToken** 存登录凭证。Token 决定「当前是谁」，所以 `user_token.user_id` 外键挂 `user.id`。

**User = 项目通用用户字段**（多数业务项目这套就能起步）：

| 字段 | 作用 |
|------|------|
| `id` | 主键 |
| `username` | 登录账号（唯一） |
| `password` | 密码哈希 |
| `employee_no` | 工号（唯一） |
| `role` | 角色 |
| `updated_at` | 更新时间 |

**UserToken：** `id`、`user_id`（外键 → `user.id`）、`token`、`expires_at`。

```python
class User(Base):
    __tablename__ = "user"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    username: Mapped[str] = mapped_column(String(50), unique=True, nullable=False)
    password: Mapped[str] = mapped_column(String(255), nullable=False)  # 存哈希
    employee_no: Mapped[str] = mapped_column(String(50), unique=True, nullable=False)
    role: Mapped[str] = mapped_column(String(20), nullable=False, default="inspector")
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, insert_default=func.now(), default=func.now, onupdate=func.now()
    )

class UserToken(Base):
    __tablename__ = "user_token"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("user.id"), nullable=False)
    token: Mapped[str] = mapped_column(String(255), unique=True, nullable=False)
    expires_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)
```

**经验：** 改模型前先 `DESC user;` 看真实列名。

#### schema

文件：`back/schema/loginuser.py`

接口是 POST，请求体要先有校验类。登录只要账号密码；开通用的 `UserRequest`（含工号）见第九章。

```python
from pydantic import BaseModel

class loginRequest(BaseModel):
    username: str
    password: str
```

**坑：** 登录请求里误塞 `employee_no`——契约别和开通混。

#### router（先写；crud 用注释占位）

文件：`back/router/loginuser.py`，前缀 `/api/user`。

**占位长这样：** 真逻辑先注释掉，先能 `return` 通一下路径。

```python
@router.post("/login")
async def login(user_data: loginRequest, db: AsyncSession = Depends(get_db)):
    # loginuser = await post_login(db, user_data.username, user_data.password)
    # if not loginuser:
    #     raise HTTPException(status_code=401, detail="用户名或密码错误")
    # token = await post_token(db, loginuser.id)
    return success_response(
        message="登录成功",
        data="data",
    )
```

从注释里就能看出接下来 crud 要提供什么：

```text
post_login(db, username, password) → User | None
post_token(db, user_id) → token 字符串
```

**坑：路由参数顺序**  
没有默认值的参数不能写在有默认值的后面。`Depends(get_db)` 有默认值，请求体参数写在它前面。

#### crud（按 router 注释里的签名实现）

文件：`back/crud/user.py`

| 函数 | 干什么（签名跟 router 对齐） |
|------|--------|
| `post_login(db, username, password)` | 查用户 → 无则 None → `verify` 失败则 None → 成功返回用户行 |
| `post_token(db, user_id)` | UUID，过期 +7 天；已有则覆盖，无则插入；**return token** |

（`post_userinfo` / `add_user` 给第九章开通用。）

```python
pwd_obj = CryptContext(schemes=["bcrypt"], deprecated="auto")

async def post_login(db: AsyncSession, username, password):
    stmt = select(User).where(User.username == username)
    result_orm = (await db.execute(stmt)).scalar_one_or_none()
    if not result_orm:
        return None
    if not pwd_obj.verify(password, result_orm.password):
        return None
    return result_orm

async def post_token(db: AsyncSession, user_id):
    token = str(uuid.uuid4())
    expires_at = datetime.now() + timedelta(days=7)
    stmt = select(UserToken).where(UserToken.user_id == user_id)
    result_orm = (await db.execute(stmt)).scalar_one_or_none()
    if result_orm:
        # 已有则覆盖（每次登录换新 token）
        result_orm.token = token
        result_orm.expires_at = expires_at
    else:
        # 没有则插入
        db.add(UserToken(user_id=user_id, token=token, expires_at=expires_at))
    await db.commit()
    return token  # 必须返回，前端才能存
```

一定要返回 `return token`，否则路由拿不到token，否则token无法创建或者更新等等。

**问过的概念**

- `scalar_one_or_none`：期望 0 或 1 条；没有 → `None`；有 → 那一行；多行会报错，所以用户名要唯一。
- `refresh`：`commit` 后把自增 id 填回对象（开通账号时会用到；登录用已有 `loginuser.id`）。
- `ForeignKey`：`user_token.user_id` 必须对应真实的 `user.id`。

**坑：passlib / bcrypt 报错**  
固定 `bcrypt==4.0.1`，在 `back` 的 `.venv` 里装。

**坑：`ModuleNotFoundError: langchain_core`（起后端时）**  
`cd back && source .venv/bin/activate` 再 `uvicorn`。

#### router 接上真 crud（取消注释）

crud 写完后，回到 `loginuser.py`：去掉注释，换成真返回。

```python
@router.post("/login")
async def login(user_data: loginRequest, db: AsyncSession = Depends(get_db)):
    loginuser = await post_login(db, user_data.username, user_data.password)
    if not loginuser:
        raise HTTPException(status_code=401, detail="用户名或密码错误")
    token = await post_token(db, loginuser.id)
    data = {
        "token": token,
        "userInfo": {
            "id": loginuser.id,
            "username": loginuser.username,
            "employee_no": loginuser.employee_no,
            "role": loginuser.role,
        },
    }
    return success_response(message="登录成功", data=data)
```

**坑：以为登录成功要把密码返回给前端再对一次**  
不对。后端 `verify` 通过就够了。返回只有 token 和 userInfo。

**挂 main：** 成功壳 / 异常处理器 / CORS 已在 **〇** 写过，这里只 `include_router(loginuser.router)`，不要再抄一遍 `success_response`。

#### 后端自测

浏览器打开 `http://127.0.0.1:8000/docs`：

1. 库里先有一个用户（SQL / 种子）。  
2. 调登录，看 200 和 data 里有没有 token、userInfo。  
3. 用错密码，应 401，message 是统一那句。  

`/docs` 登录通了，再写登录页。

---

### 1.3 前端登录页

| 模式 | 标题 | 表单 | 操作 |
|------|------|------|------|
| 登录 | 登录 | 账号、密码 | 提交 → 存 token → `/project` |

页面顺序：

```text
1. 配置接口（api/login.js：`post_login`）
2. 路由表加 `/login`（App/main 见〇）
3. 配置组件 Login.vue：结构 → 样式 → onLogin
4. 联调：空值、成功跳转、失败提示
```

#### 配置接口

文件：`front/front_mini_agent/src/api/login.js`  
依赖：`src/utils/request.js`（axios 实例）；`/api` 代理见 **〇.2**，这里不再配。

```js
import request from '@/utils/request'

export const post_login = (data) => {
  return request.post('/api/user/login', data)
}
```

**坑：`request.post(url, { data })` 多套一层**  
后端收到的字段会在 `data` 嵌套里，Pydantic 对不上。  
**正确：** `request.post(url, data)`，`data` 就是 `{ username, password }`。



后端没开时，前端会网络错误，进 `catch`。

#### 配置路由（只加登录相关 path）

`App.vue` / `main.js` / Element Plus 见 **〇.2**，这里只往 `router/index.js` 加登录入口：

```js
// router/index.js（片段）
{ path: '/', redirect: '/login' },
{
  path: '/login',
  name: 'login',
  component: () => import('@/views/Login.vue'),
},
```

（早期还有独立 `/chat`；进 Layout 后聊天作子路由，见第八章。）

**思路：`useRouter` / `useRoute`**  
- `useRouter()`：跳转，`router.push('/project')`  
- `useRoute()`：读当前路径、参数  
script 里必须先 `const router = useRouter()`。

#### 配置组件 `Login.vue`

顺序：**template 结构 → style → script（onLogin）**。

框架：

```text
div.login-page
  └─ div.login-card
       ├─ 品牌区（产品名）
       ├─ h2.title「登录」
       └─ el-form：账号、密码、登录按钮
```

**template：**

```vue
<template>
  <div class="login-page">
    <div class="login-card">
      <div class="login-brand">
        <div class="mark">等</div>
        <div>
          <h1 class="product">等保整改跟踪</h1>
          <p class="hint">机构内部整改进度工作台</p>
        </div>
      </div>
      <h2 class="title">登录</h2>
      <el-form class="form" label-position="top" @submit.prevent="onLogin">
        <el-form-item label="账号">
          <el-input v-model="form.username" placeholder="用户名" clearable />
        </el-form-item>
        <el-form-item label="密码">
          <el-input
            v-model="form.password"
            type="password"
            placeholder="密码"
            show-password
          />
        </el-form-item>
        <el-button type="primary" native-type="submit" :loading="loading" class="submit">
          登录
        </el-button>
      </el-form>
    </div>
  </div>
</template>
```

要点：`v-model` 绑表单；**`@submit.prevent="onLogin"` + 按钮 `native-type="submit"`**（回车/点登录走同一条提交路径，见 **十三.4**）；`:loading="loading"` 防连点。  
（旧写法只 `@click="onLogin"` 时，密码框回车往往进不了登录逻辑。）

居中卡片写在 `style scoped` 即可；注意闭合括号。  
**坑：Vite `Unclosed block`** —— `{` 少写 `}` 整页编译失败。

**script：**

```js
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { post_login } from '@/api/login'

const router = useRouter()
const loading = ref(false)
const form = ref({ username: '', password: '' })

async function onLogin() {
  if (!form.value.username.trim() || !form.value.password) {
    ElMessage.warning('请输入账号和密码')
    return
  }
  loading.value = true
  try {
    const res = await post_login({
      username: form.value.username,
      password: form.value.password,
    })
    const payload = res.data?.data
    if (payload?.token) {
      localStorage.setItem('token', payload.token)
      localStorage.setItem('userInfo', JSON.stringify(payload.userInfo ?? {}))
    }
    ElMessage.success(res.data?.message || '登录成功')
    await router.push('/project')
  } catch (e) {
    ElMessage.error(e.response?.data?.message || '登录失败')
  } finally {
    loading.value = false
  }
}
```

函数里每一步：

```text
空值先拦 → loading=true → 函数里现组 body → await
  → 有 token 则 localStorage → push('/project')
  → catch 读统一 message → finally loading=false
```

**坑：** `async const onLogin` 不合法；`logindata` 写在函数外面会拿空值；业务数据在 `res.data.data`。

**为什么存 userInfo？**  
token 带请求；userInfo 给侧栏判经理、状态机显按钮。`localStorage` 只能存字符串 → `JSON.stringify`。

#### 联调检查清单

| 步骤 | 预期 |
|------|------|
| 后端 venv + uvicorn 开着 | `/docs` 能打开 |
| `pnpm run dev` | `localhost:5173` → `/login` |
| 空着点登录 | 警告「请输入账号和密码」，不发请求 |
| 已有用户登录 | 成功提示，跳到 `/project`，Application 里有 token、userInfo |
| 故意输错密码 | 提示「用户名或密码错误」，停在登录页 |

---

### 1.4 本段浓缩

1. 先契约，再后端 `/login`，再 `/docs`，最后登录页。  
2. 后端：模型 → schema → **router（可占位）→ crud → 换真调用** → main 挂路由（壳/异常见〇）。  
3. 前端：接口 → 路由 → 组件（结构 → 样式 → 请求）。  
4. 坑优先查：拼写、括号、venv、字段名和库是否一致。

**下一步：** 项目 / 差距 / 状态机（五）起。

---

## 五、项目列表到差距状态机

> **第一章**是登录整段。本章是登录之后按**实际写代码的顺序**记的。知识点写在对应实施步骤里，不另开「纯知识点」大节。①～⑦ 是状态联调步骤编号，不是文档大题号。

## 整条线怎么串起来（先看因果，再看分节）

每一小步都不是「突然想到再写一块」，而是：**上一步做完之后，演示还缺什么 → 所以下一步补什么**。

```text
登录能进系统了
  → 但还没有「等保业务主页」：不知道有哪些被测项目
  → 5.1 定产品目标：先项目、再差距、再改状态
  → 5.2 先建项目表 project（先别建差距表）
  → 5.3 拉全部项目列表 + 页面渲染
  → 5.4 点一行：动态路由跳进差距页——先做空壳页也行
  → 5.5 空页要填数据：再建 gap_item（project_id 外键挂上）
  → 5.6 用 route.params.id → ?project_id= 拉差距列表并渲染
  → 5.7 列表太扁：做抽屉壳（先看全、按钮占位）
  → 5.8 真改库：历史表 + TRANSITIONS + crud
  → 5.9 开 HTTP → /docs → api → 抽屉接真按钮
  → 5.10 本段收束；角色/token 故意后置 → 进第六章
```

复习时先顺着这条「缺什么 → 补什么」走一遍，再进下面各节细节。

### 5.1 这段要做成什么（产品目标）

**承接：** 登录段结束，人能进系统，但还没有等保整改的主业务界面。  
**所以先想清楚：** 用户进来之后要干什么？——不是先聊 Agent，而是：**选项目 → 看差距 → 推进状态并留痕迹**。

登录 → `/project` 项目列表（可按名称搜）→ 点击一行 → `/project/:id/gaps` 差距列表 → 点一条打开抽屉 → 代录 / 提交 / 退回 / 通过 / 关闭（后端改 `status` + 写操作历史）。

**版本怎么定（汇报用，避免把 Agent 吹过头）：**

- **第一版主线：** 整改跟踪业务 + 状态机（当前重点）。  

现有「Agent 对话」只是最小聊天闭环（能聊、聊天记录落库），**尚未**把等保业务库接到 Agent，也**尚未**上向量库/RAG。业务完成度与「智能问答完成度」要分开说。

---

### 5.2 先落库：项目表（因为只是测试种子数据，所以可以sql加几条)

以后在页面中添加项目的时候，即可用到remark。

**承接：** 5.1 定了「先选项目，再进差距」。  
**缺什么：** 库里还没有项目，后面列表无处可查。  
**所以这一步：** 只建 `project`（先别急着建差距表）。

| 字段 | 作用 |
|------|------|
| `id` | 主键 |
| `name` | 项目名称 |
| `remark` | 备注（可空） |
| `create_at` | 创建时间（基类） |

```python
class Project(Base):
    __tablename__ = "project"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    remark: Mapped[str | None] = mapped_column(String(255), nullable=True)
```

**本步做完：** 库里能插几条项目种子；还没有差距表、还没有列表页。

**连接点：** 后面差距要挂在项目上，需要 `project.id`；但**现在还不用建差距表**——先把「看见项目」做通。

**所以下一步：** 项目列表接口 + 页面渲染（5.3）。

---

### 5.3 项目列表：拉全部 + 渲染

**承接：** 5.2 库里已有 `project`（含种子里的 `remark` 等字段）。  
**缺什么：** 没有 API、没有页面，演示走不到「我的项目」。  
**所以这一步：** 和登录同一套路——先 `/docs` 通列表，再做 `project.vue` 渲染出来。

**本步范围（重要）：** 此时**还没有** `project_member`，也**不做**「按登录用户过滤项目」。  
`getprojects` = 查 `project` 表；不传 `name` 或空 → **库里有的项目全部返回**；有 `name` → `LIKE` 模糊搜。  
成员表、核查员只看自己的项目 → **第七章**再改签名、加过滤。复习时不要拿现在的 `getprojects(name, db, user_id, role)` 倒灌进 5.3。

#### 后端

文件：`back/router/project.py`（前缀 `/api/project`）、`back/crud/projects.py`。

##### router（先写；crud 可先占位）

本步查询参数只有可选的 `name`。返回里的 `remark` 等是**从库行读出**，不是请求传进来的。

```python
# back/router/project.py
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from config.request import get_db
from utils.response import success_response
from crud.projects import getprojects

router = APIRouter(prefix="/api/project", tags=["list"])

@router.get("/list")
async def getprojectlist(
    name: str | None = None,
    db: AsyncSession = Depends(get_db),
):
    # rows = await getprojects(name, db)  # crud 写完再取消注释
    rows = await getprojects(name, db)
    data = []
    for r in rows:
        data.append(
            {
                "id": r.id,
                "name": r.name,
                "remark": r.remark,       # 库里已有（种子），列表读出
                "create_at": r.create_at,
            }
        )
    return success_response(message="返回项目列表成功", data=data)
```

从路由能看出 crud 签名：`getprojects(name, db) → list[Project]`。  
（本步**还不需要** `Depends(get_current_user)` / `user_id` / `role`——那是六、七章。）

##### crud（后写；只碰 project 表）

```python
# back/crud/projects.py
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from model.chat import Project

async def getprojects(name: str | None, db: AsyncSession):
    stmt = select(Project).order_by(Project.create_at.desc())
    if name:
        stmt = stmt.where(Project.name.like(f"%{name}%"))
    result = await db.execute(stmt)
    return result.scalars().all()
```

- `name` 空 / 不传：全表项目。  
- 有 `name`：模糊搜。  
- **没有** `ProjectMember`、**没有** `user_id` 参数。

##### 挂路由

`main.py`：`app.include_router(project.router)`（若尚未挂）。  
`/docs` 调 `GET /api/project/list`，有种子就能看到带 `remark` 的列表。

#### 前端

##### api

```js
// api/projects.js
import request from '@/utils/request'

export const getprojects = (params) => {
  return request.get('/api/project/list', { params })
}
```

##### 路由

```js
// router/index.js（当时独立 path；进 Layout 后改成 children，见第八章）
{ path: '/project', name: 'project', component: () => import('@/views/project/project.vue') }
```

##### 页面要点

```js
// project.vue —— script setup
const projects = ref([])
const keyword = ref('')
const loading = ref(false)

const onprojects = async () => {
  loading.value = true
  try {
    const params = { name: keyword.value.trim() || '' }
    const res = await getprojects(params)
    projects.value = res.data?.data ?? []
  } finally {
    loading.value = false
  }
}
onMounted(onprojects)
```

模板：搜索框 + 按钮调 `onprojects`；`v-loading`；空态「暂无项目」；网格展示 `name` / `remark` / `create_at`。

做法与登录页同一套路：**接口文件 → 路由加 path → 页面里请求**（后端侧则是 **router → crud**，不要反）。

**本步做完：** 能看见项目列表（当时是全库项目）。点一行还无处可去（或先不点）。

**所以下一步：** 点一行要进「该项目的差距」——先配动态路由，差距页可以先是空壳（5.4）。

---

### 5.4 点进差距页（先空壳也行）

**承接：** 5.3 项目列表已通。  
**缺什么：** 点击项目没有下一页；URL 还要能区分「进的是项目 1 还是项目 3」。  
**所以这一步：** **只做前端跳转 + 空壳页**，证明「点项目 → 地址栏出现 `/project/3/gaps`」通了。  
**本步不做：** 不建 `gap_item`、不写差距 list 接口、空页里**先不调接口**。落库是 **5.5**，拉列表填满是 **5.6**——顺序不能跳。

#### 后端

**本步不做。** 没有新 API；项目 list 继续用 5.3。  
（空壳页只读前端路由里的 `:id`，还不请求后端。）

#### 前端

##### 1. 路由表加动态 path

文件：`router/index.js`（当时可与 `/project` 平级；进 Layout 后放进 `children`，path 写成 `project/:id/gaps`，见第八章）

```js
{
  path: '/project/:id/gaps',
  name: 'gap',
  component: () => import('@/views/project/gaps.vue'),
}
```

- `:id` 是占位坑；真正打开时变成 `1`、`3` 等。  
- **坑：** `push` 的路径必须和这里写的一致（不要一边 `/gaps` 一边 `/gap`）。

##### 2. 项目列表：点一行再跳（`useRouter` + `push`）

文件：`views/project/project.vue`

侧栏固定地址用 `<router-link>`；**列表进某一条**要先拿到 `item.id` 再拼路径，用脚本里的 `push`：

```js
import { useRouter } from 'vue-router'
const router = useRouter()

function goGaps(projectId) {
  // 把真实项目主键填进 :id
  router.push(`/project/${projectId}/gaps`)
}
```

模板行上：

```html
<div
  v-for="item in projects"
  :key="item.id"
  class="card"
  @click="goGaps(item.id)"
>
  ...
</div>
```

| 场景 | 用啥 |
|------|------|
| 侧栏固定地址 | `<router-link to="/project">` |
| 列表进差距（带动态 id） | `router.push(\`/project/${id}/gaps\`)` |

| | `useRouter()` | `useRoute()` |
|---|---|---|
| 用途 | `push` 去哪 | 读当前页的 `params` / `path` |
| 本步 | 列表页 `goGaps` | **空壳页**读 `route.params.id` |

##### 3. 空壳页 `gaps.vue`（本步完整实现；先别接接口）

新建 `views/project/gaps.vue`。目标：**能打开、能看见当前项目 id**；里面写「差距列表稍后接入」即可。

```vue
<script setup>
import { useRoute } from 'vue-router'

const route = useRoute()
// params 里常常是字符串；5.6 请求接口时再 Number(route.params.id)
</script>

<template>
  <div class="box">
    <h2 class="title">差距列表</h2>
    <p class="sub">项目 ID：{{ route.params.id }}（空壳：尚未拉差距数据）</p>
    <p>本步只验证跳转。建表见 5.5，列表接口与渲染见 5.6。</p>
  </div>
</template>

<style scoped>
.box {
  box-sizing: border-box;
  width: 900px;
  margin: 60px auto 0;
  padding: 12px;
  border: 1px solid #000;
  min-height: 70vh;
}
.title {
  text-align: center;
}
.sub {
  color: #666;
}
</style>
```

**自测：** 打开项目列表 → 点某一行 → 地址栏变成 `/project/3/gaps`（数字随点的项目变）→ 页上显示对应「项目 ID」。  
还没有差距数据、也没有报错——这才叫 **5.4 做完**。

**本步做完：** 点项目能进空壳差距页，URL 带项目 id。

**连接点：** 空页以后要列出「这个项目下的差距」，才需要库表 `gap_item`（`project_id` 外键）——那是下一节，不是这一节。

**所以下一步：** 建差距表（5.5），再写 list 接口并把空壳填满（5.6）。

---

### 5.5 再落库：差距表（外键挂到项目）

**承接：** 5.4 已经能点进空的差距页，地址栏有项目 id（跳转已通，空壳代码已落）。  
**缺什么：** 库里还没有差距项，空页没法填真数据。  
**所以这一步：** 建 `gap_item`，**`project_id` → `project.id`**。  
**提醒：** 若 5.4 的空壳 / `goGaps` / 动态路由还没写完，先回去补 5.4，不要跳到建表。

| 字段 | 作用 |
|------|------|
| `id` | 主键 |
| `project_id` | 外键，属于哪个项目 |
| `title` | 差距标题 |
| `status` | 当前状态（默认 `待整改`） |
| `category` / `risk_level` | 类别、风险 |
| `reason` / `requirement` | 原因、整改要求 |
| `create_at` | 创建时间（基类） |

```python
class GapItem(Base):
    __tablename__ = "gap_item"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    project_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("project.id"), nullable=False
    )
    title: Mapped[str] = mapped_column(String(200), nullable=False)
    status: Mapped[str] = mapped_column(String(20), nullable=False, default="待整改")
    category: Mapped[str | None] = mapped_column(String(50), nullable=True)
    risk_level: Mapped[str | None] = mapped_column(String(20), nullable=True)
    reason: Mapped[str | None] = mapped_column(Text, nullable=True)
    requirement: Mapped[str | None] = mapped_column(Text, nullable=True)
```

#### 外键为什么这样写

差距**属于**某个项目，不要每行再抄项目名。写成 `project_id = 3` 即可。  
以后历史表的 `gap_id`、操作人的 `actor_id` 同理——**关联 + 不重复存**。

#### 字段怎么想出来（三句触发法）

1. **成功之后用户能看见什么？** → 展示字段。  
2. **失败时凭什么拒绝？** → 校验、状态是否合法。  
3. **三个月后翻旧账靠什么？** → 历史、外键、时间、说明。  

**所以下一步：** 表有了，把空壳页接上列表接口（5.6）。

---

### 5.6 差距列表页：读 `route.params.id`，请求带 `project_id`

**承接：** 5.4 已能进空壳页；5.5 库里已有 `gap_item`。  
**缺什么：** 空页还没按「当前项目」去拉差距并渲染。  
**所以这一步：** 差距列表接口 + `gaps.vue` 里 `Number(route.params.id)` → `{ project_id }`。

#### 实际代码怎么写

1. 后端：**先** `GET /api/project/list/gap?project_id=`（router），**再** crud `getlist_gaps`，按 `GapItem.project_id` 过滤。  
2. 前端 APIji接收返回的数据：

```js
export const getgap_project_id = (params) => {
  return request.get('/api/project/list/gap', { params })
}
```

3. `gaps.vue` 必须用 **`<script setup>`**（普通 `<script>` 顶层调 `useRoute`/`onMounted` 不合法）：

```js
const route = useRoute()
const gaps = ref([])
const res = await getgap_project_id({
  project_id: Number(route.params.id),
})
gaps.value = res.data?.data ?? []
```

模板用 `v-for` 展示 title、status、category、risk_level、create_at 等。

#### 本步踩过的坑  

**所以下一步：** 差距能列出来了，但一行摘要看不全，也没法在同页改状态 → 做抽屉壳（5.7），按钮先占位。

---

### 5.7 抽屉壳（同页看一条差距；知识点写在壳里）

**承接：** 5.6 差距列表已通。  
**缺什么：** 原因/要求挤在列表里难看；演示要「打开该条」再操作，又不必再开一层路由。  
**所以这一步：** `el-drawer`，点行打开；先展示完整信息，状态按钮可以先假点。

#### 为什么用抽屉，而不是再开详情路由

差距列表写好后，希望对**每一条**差距直观看到完整信息与状态，并在同页准备改状态。  
用 Element Plus 的 **`el-drawer`**：不离开当前页、一般不换路由，从侧面拉出面板。  

MVP **不做** `/project/:id/gaps/:gapId` 独立详情页；字段不多时，列表 + 抽屉足够。状态按钮先做壳，真接口见后面 5.8～5.9。

#### 抽屉概念（嵌在本步）

**是什么：** 页面里可以拉出来的侧窗/面板。  
**干什么：** 展示当前选中这一条的详情；在抽屉里操作（填说明、点提交等），从而改本页相关状态或数据；改完通常调接口再刷新列表。  
**和跳详情页的差别：** 详情页换 URL、换整页；抽屉是**同页弹层**。抽屉本身只是 UI 容器，真改数据靠里面的按钮调接口。

#### 两个核心状态

| 变量 | 作用 |
|------|------|
| `drawerVisible` | 抽屉开/关（布尔），默认 `false` |
| `current` | 当前点开的那**一条**差距对象，默认 `null` |

管的是「当前这条差距」，不是整个项目列表，也不是把整个 `gaps` 数组绑进抽屉。

#### 1. 开关

- 模板：`v-model="drawerVisible"` 双向绑定显隐。  
- 打开：脚本里 `drawerVisible.value = true`（在点击行时）。  
- 关闭：点叉号 / 遮罩时组件把 `drawerVisible` 改回 `false`。  

#### 2. 绑定「当前这条」

列表 `v-for="item in gaps"` 时，点击写在**每一行**：`@click="openDrawer(item)"`。  
`item` 是这一行整条数据，传入函数：

```js
function openDrawer(item) {
  current.value = { ...item }   // 先拿到数据（浅拷贝，避免和列表引用搅在一起）
  drawerVisible.value = true    // 再打开
  // 接上历史后：再 await loadLogs(item.id)
}
```

**顺序必须：先给 `current` 赋值，再开抽屉**，模板才能用 `current.title`、`current.status` 等渲染。  
这不是必须做 JS「解构」`const { title } = item`；本质是**整条对象赋给 `current`，用 `current.xxx` 展示**。模板侧建议 `v-if="current"`。

#### 3. 和「项目」的分层（易混）

- `route.params.id`：当前进的是哪个**项目**（用来拉该项目下差距列表）。  
- 抽屉里的 `current`：点的是列表里哪一条**差距**。  

先按项目进列表，再按差距开抽屉。

#### 4. 壳阶段模板与细节

- 列表：摘要列（标题、状态、类别、风险、时间）+ `cursor: pointer`。  
- 抽屉内：完整字段（原因、要求等）+ 五个按钮占位（代录/提交/退回/通过/关闭）；壳阶段可 `ElMessage.info('接口稍后接入')`。  
- `destroy-on-close`：关闭时销毁内部 DOM，下次更干净。  
- `@close`：`current = null`；有历史后一并 `logs = []`，避免下次闪出上一条。  

#### 壳阶段做完标准

点任意一行 → 抽屉打开且字段对应该行；关掉再开别的行不串数据；按钮可先占位。

**所以下一步：** 壳有了，点「代录」还不能真改库；演示还要求「历史里能看到两次」→ 不能只改 `status`，要历史表 + 状态机 crud（5.8）。

---

### 5.8 状态落库与状态机（历史表 → TRANSITIONS → crud）

**承接：** 5.7 抽屉能打开，按钮还是空壳。  
**缺什么：** 没有「合法怎么变」的规则，也没有操作流水；只改一个字段无法证明谁何时做了什么。  
**所以这一步：** `gap_action_log` + `TRANSITIONS` + `apply_gap_action`（改仪表盘 + 记流水）。

产品要的不只是「当前 status」，还要「历史里能看到代录、提交两次」。所以要两张表配合，但分工不是「一张只存以前、一张存前后」：

| 表 | 存什么 | 像什么 |
|----|--------|--------|
| `gap_item`（上的 `status`） | **只要当前这一份**读数 | 仪表盘现在指到哪 |
| `gap_action_log` | **每一次操作一行**：谁、何时、动作、`from_status`→`to_status`、说明 | 行车记录仪 |

要点记清楚：

- 「以前的状态」**不单独占一张表**，写在流水行的 **`from_status`** 里。  
- 「现在的状态」在 **`gap_item.status`**，同时也是这次流水的 **`to_status`**。  
- 每改一次状态，就要：**拨仪表盘（改当前 status）+ 记一笔流水（插 log）**，不能只做一半。  

一次合法操作 = **改仪表盘 + 记一笔流水**：

1. 改 `gap_item.status`（拨到新值）  
2. 往 `gap_action_log` 插一行（记下这笔：谁、动作、从哪到哪、说明）  

只改仪表盘 → 没有历史可翻；只写流水不改仪表盘 → 列表上的状态不会变。两步应在同一次提交里完成。

#### 历史表字段（接 5.5 的三句触发）

| 字段 | 含义 |
|------|------|
| `id` | 本条历史主键 |
| `gap_id` | 哪条差距 → `gap_item.id` |
| `actor_id` | 谁操作 → `user.id` |
| `action` | record / submit / reject / approve / close |
| `from_status` / `to_status` | 从哪到哪 |
| `comment` | 代录说明、退回原因等 |
| `create_at` | 何时 |

MySQL 建表 + `model/chat.py` 写 `GapActionLog`；**不必改** `gap_item` 表结构。

#### 合法边（契约）与 `TRANSITIONS`

```text
待整改 --record--> 整改中 --submit--> 待复核 --approve--> 已通过
                ^                    |
                +------ reject ------+
待整改 --close--> 已关闭
```

| 动作 | 从 → 到 | 必填说明 |
|------|---------|----------|
| record 代录 | 待整改 → 整改中 | 整改说明 |
| submit 提交 | 整改中 → 待复核 | 可空 |
| reject 退回 | 待复核 → 整改中 | **退回原因必填** |
| approve 通过 | 待复核 → 已通过 | 终态，不能再退回/关 |
| close 关闭 | 待整改 → 已关闭 | **不是**任意一步都能关 |

代码里用字典表达「这个动作会造成什么」：

```python
TRANSITIONS = {
    ("待整改", "record"): "整改中",
    ("整改中", "submit"): "待复核",
    ("待复核", "reject"): "整改中",
    ("待复核", "approve"): "已通过",
    ("待整改", "close"): "已关闭",
}
```

键 = `(当前状态, 动作)`，值 = 下一状态；**查不到键 = 非法，直接拒绝**。  
这是业务里常见的**有限状态机（FSM）**写法：状态集合 + 转移表 + 执行器；比前端随便改下拉框正规，比上工作流引擎轻。

#### `apply_gap_action` 执行流水线（`crud/projects.py`）

1. `get_gap_by_id`；没有 → `HTTPException 404`「差距项不存在」。  
2. `key = (gap.status, action)`；不在 `TRANSITIONS` → `400`「当前状态不能执行该动作」。  
3. `reject` / `record` 且 comment 空 → `400`。  
4. `from_status = gap.status`，`gap.status = TRANSITIONS[key]`。  
5. `db.add(GapActionLog(...))`。  
6. `commit` + `refresh`，返回 gap。  

**字典管规矩，函数管执行。**

**所以下一步：** crud 写在后端肚子里，抽屉按钮调不到 → 开 HTTP，用 `/docs` 测通，再封装前端、接按钮（5.9）。

---

### 5.9 开 HTTP 接口、自测、前端封装、抽屉接真按钮（①～⑦串起来）

**承接：** 5.8 状态机函数已能改库写 log。  
**缺什么：** 没有门牌给浏览器；前端也还在「接口稍后接入」。  
**所以这一步：** ④ 开 action/logs → ⑤ 自测 → ⑥ api 封装 → ⑦ 抽屉真调并刷新列表/current/历史。

抽屉壳是 ①；历史表+模型是 ②；TRANSITIONS+函数是 ③。下面 ④～⑦ 把门打开并接到按钮。

#### ④ 开接口（`router/project.py` + `schema/gap.py`）

crud 写在后端内部，前端和浏览器打不到，需要 HTTP 门牌（**前后端共同约定的同一串 URL**；不是「只写给前端」）：

| 方法 | 路径 | 作用 |
|------|------|------|
| POST | `/api/project/gap/{gap_id}/action` | 执行动作 |
| GET | `/api/project/gap/{gap_id}/logs` | 拉历史 |

- **路径 `{gap_id}`**：对哪一条差距（差距项主键，不是项目 id）。  
- **Body（当时过渡写法）**：做什么，例如 `{ "action": "record", "comment": "已改双因素…", "actor_id": 1 }`。  

路径定对象，Body 定动作。`GapActionRequest` 约束 Body。路由里调 `apply_gap_action` / `list_gap_logs`，用 `success_response` 返回。  

角色校验（张三不能通过）当时故意后置；这一步先保证状态合法就能改 + 写 log。`actor_id` 暂可 Body 传入——**第六章会改成从 token 取当前用户，Body 不再带 actor_id。**

#### ⑤ `/docs` 或 curl 自测（先不靠前端）

前端一接就同时有 Vue、代理、按钮，出错难定位。先证明单后端状态机对：

1. 确认新路由已加载（无 `--reload` 则重启）。  
2. 对一条「待整改」POST `record` + comment → 应变「整改中」，logs 多一行。  
3. 再 `submit` → 「待复核」。  
4. GET logs 能看见两次。  
5. 故意非法（已通过再 approve）→ 应 400。  

#### ⑥ `api/projects.js` 封装

```js
postGapAction(gapId, data)  // POST `/api/project/gap/${gapId}/action`
getGapLogs(gapId)           // GET  `/api/project/gap/${gapId}/logs`
```

页面不散落 URL；`gapId` 是 **`current.id`（差距）**，不要传 `route.params.id`（项目）。

#### ⑦ 抽屉按钮接真接口（`gaps.vue`）

| 按钮文案 | action |
|----------|--------|
| 代录说明 | `record` |
| 提交复核 | `submit` |
| 退回 | `reject` |
| 通过 | `approve` |
| 关闭 | `close` |

- 代录 / 退回：先 `ElMessageBox.prompt`，空则不许确定；取消则不发请求。  
- `postGapAction(current.id, { action, comment, actor_id })`；`actor_id` 暂从 `localStorage.userInfo.id` 取，缺省可用 `1`（过渡；第六章删掉）。  
- **成功后三处要一致：** 提示成功 → **刷新差距列表** → **更新抽屉 `current.status`** → **再拉 logs**。缺刷新列表会出现「抽屉变了、行上还是旧状态」。  
- 失败用后端 `message`/`detail` 提示（非法流转也能在页面上验证）。  
- 打开抽屉时 `loadLogs`；关闭清 `current` 与 `logs`。  

#### ①～⑦ 总览（实施步骤，不是大题号）

| 步 | 做什么 | 主要落点 |
|----|--------|----------|
| ① | 抽屉壳 | `gaps.vue` |
| ② | 历史表 + 模型 | MySQL + `GapActionLog` |
| ③ | 转移表 + 执行函数 | `crud/projects.py` |
| ④ | HTTP 开门 | router + schema |
| ⑤ | 后端自测 | `/docs` / curl |
| ⑥ | 前端封装请求 | `projects.js` |
| ⑦ | 按钮联调并刷新三处 UI | `gaps.vue` |

① 纯前端壳；②～⑤ 后端主链路；⑥～⑦ 把壳接到真状态机。角色藏按钮、token 强制身份排在七步之后（见**第六章**）。

#### 联调做完标准

登录进项目 → 点一条 → 代录（填说明）→ 列表与抽屉变为整改中、历史多一行 → 再提交变为待复核；非法操作有明确错误提示。

**所以下一步（本段之外）：** 主链路能演示了；剧本里还缺「核查人员不能点通过、接口也要拒绝」——进入第六章。

---

### 5.10 本段做法浓缩 + 还没做的

**承接：** 5.9 联调标准达到。  
**本段停在哪：** 「选项目 → 看差距 → 抽屉推进状态并看历史」已通。  
**第五章故意后置、第六章已接上：** 带 token 认人 + 按 `role` 管动作。  
**仍后置（章内当时写法，部分已提前/改口）：** 成员与新建（十一已做）；**录入、退出登录、守卫、401 跳转（十三已做）**；Agent 接业务库、Redis、RAG → 第二版。跨项目汇总没意义，不做。

1. 整段因果就是上面那条链：缺什么补什么，不要跳着写。  
2. 每改一次状态 = 改 `gap_item.status`（当前）+ 插 `gap_action_log`（流水里带 from/to、操作者等）；「以前的状态」在 log 的 `from_status`，不另建第三张表。  
3. 项目 id（路由 `:id`）和差距 id（action 路径）分清；页面 path 与接口 query 分清。  
4. `router.push` 的 path 必须与 `router/index.js` 完全一致。  
5. 后端改路由后确认进程已加载；教学阶段建议 uvicorn 加 `--reload`。  
6. **第五章完成标准不含角色**；角色层见第六章。成员过滤、新建项目等再往后。  

---

## 六、状态机之后：身份与角色怎么管流程

> 五章把「状态能不能变」做完了。本章接的是剧本里那句：**核查人员不能点通过，接口也要拒绝**。  
> 按实际写代码顺序：6.1～6.7。知识点嵌在步骤里。

## 整条线怎么串起来（先看因果，再看分节）

第五章结束时，任意人只要知道 `gap_id`，Body 里塞个 `actor_id`，就能调五个动作——**状态合法就过**。演示剧本却要求：张三（inspector）代录/提交可以，点「通过」必须失败；经理才能 reject/approve/close。

```text
状态机已通（能改 status + 写 log）
  → 但「谁在操作」靠前端自报 actor_id，可伪造
  → 6.1 先分清：功能权限（角色）≠ 数据权限（成员）；本章只做前者
  → 6.2 前端每次请求自动带 Authorization: Bearer <token>
  → 6.3 后端用 token 查出真正的 User（先 get_current_user，再调 get_user_by_token）
  → 6.4 action 路由挂 Depends(get_current_user)，actor_id 改从 current_user.id 取
  → 6.5 状态机之外加 ACTION_ROLES：先判角色 403，再判转移 400
  → 6.6 前端按 userInfo.role 藏按钮；Body 去掉 actor_id
  → 6.7 本段收束；成员过滤 / 新建项目等仍后置
```

复习口诀：**token 认人 → role 管动作 → TRANSITIONS 管流转。三层各管一件事，不要混成一个 if。**

---

### 6.1 承接第五章：缺的是「管控」，不是再改状态边

**承接：** 5.9/5.10 联调标准达到——合法流转能走通，非法状态会 400。  
**演示还缺什么：** 「张三点通过 → 必须失败」；失败原因是**角色不对**，不是状态不对。  
**所以这一章：** 在状态机外面再加一层「这个角色允不允许调这个动作」。

#### 两层拒绝，不要揉成一种

| 层 | 问的问题 | 不通过时 | 例子 |
|----|----------|----------|------|
| 角色（本章） | 你这种人能不能做这个动作？ | **403** 无权 | inspector 调 approve |
| 状态机（五章） | 当前 status 能不能接这个动作？ | **400** 非法流转 | 已通过再 approve；待整改直接 submit |

顺序必须是：**先角色，再状态**。  
否则 inspector 在「待复核」时点通过，若只查 TRANSITIONS，会先变成「已通过」——角色层永远拦不到「状态合法但人不该做」的情况。

#### 功能权限 vs 数据权限（易混，本章只做前半）

产品里其实有两种「不能」：

| 种类 | 含义 | 靠什么 | 本章？ |
|------|------|--------|--------|
| **功能权限（角色）** | 核查人员永远不能「通过」；经理永远不能「代录」 | `user.role` + `ACTION_ROLES` | **做** |
| **数据权限（成员）** | 同样是 inspector，李四看不见甲项目的差距 | `project_member` 表过滤 | **故意后置** |

答辩时可以分清：  
- 「为什么核查人员不能点通过」→ 答角色层（本章）。  
- 「为什么李四问甲项目答没有」→ 答成员层（下一阶段）。  

**不要**用「每个角色一个 token」这种怪思路。Token 只证明「你是哪个用户」；角色是用户行上的一个字段，查到 User 就知道了。

**所以下一步：** 后端要认人，前端得先把登录时拿到的 token 每次请求带上（6.2）。

---

### 6.2 前端：请求拦截器自动带 `Authorization`

> **拦截器代码与写法见 〇.2**，本章不重贴。这里只验收「接到业务时头有没有带上」。

**承接：** 登录成功时已经 `localStorage.setItem('token', ...)`（见**第一章 1.3**）。  
**本步验收：** 登录后打开差距页，Network 里业务请求应有 `Authorization: Bearer ...`。  
没看到 → 查 localStorage 键名、是否登录写入、拦截器是否挂在真正被 import 的那个 instance 上。

**所以下一步：** 头带上了，后端还没人读 → 写「用 token 查用户」（6.3）。

---

### 6.3 后端：token → User（Depends 入口 + crud 查库）

> **完整代码见 〇.3**（`get_current_user` / `get_user_by_token`），本章不重贴。这里只记「接到状态机时怎么用」。

**承接：** 6.2 请求已带头。  
**本步要懂的调用顺序：**

```text
请求进来
  → get_current_user（读 Header、剥 Bearer）
  → get_user_by_token（查库、过期、换 User）
  → 路由拿到 current_user；失败则 401，路由函数不执行
```

| 顺序 | 函数 | 文件 | 职责 |
|------|------|------|------|
| ① | `get_current_user` | `utils/auth.py` | HTTP 层：取头、剥 Bearer、调 crud、raise 401 |
| ② | `get_user_by_token` | `crud/user.py` | 数据层：凭证是否存在/过期，再查 User |

路由只写 `current_user: User = Depends(get_current_user)`，不要每个业务自己解析 Header。

**本步验收：** 带错/空 token → 401；带登录真 token → 依赖通过。

**所以下一步：** 挂到 action 路由，删掉 Body 里的假 `actor_id`（6.4）。

---

### 6.4 action 路由：用当前用户，删掉自报身份

**承接：** 6.3 已有 `get_current_user`。  
**缺什么：** `POST .../action` 仍可能从 Body 读 `actor_id`，或路由上还没挂依赖；历史里的操作者可以造假。  
**所以这一步：** 路由强制登录；`actor_id=current_user.id`；schema 去掉 `actor_id`。

#### 路由（`router/project.py`）

注意：同一路径只能留**一个** `gap_action`。联调时若先后写过「无鉴权版」和「有鉴权版」，FastAPI 会注册重复，行为混乱——**删掉旧版，只留带 `Depends` 的一份**。

```python
@router.post("/gap/{gap_id}/action")
async def gap_action(
    gap_id: int,
    body: GapActionRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    gap = await apply_gap_action(
        db,
        gap_id=gap_id,
        action=body.action,
        actor_id=current_user.id,
        role=current_user.role,   # 下一节真正用到
        comment=body.comment,
    )
    return success_response(
        message="状态更新成功",
        data={
            "id": gap.id,
            "status": gap.status,
            "project_id": gap.project_id,
            "title": gap.title,
            "actor_id": current_user.id,
            "role": current_user.role,
        },
    )
```

#### schema（`schema/gap.py`）

```python
class GapActionRequest(BaseModel):
    action: str
    comment: str | None = None
    # 不再要 actor_id：操作人只能来自 token
```

#### 因果一句话

```text
以前：前端说「我是 1 号」→ 后端信了 → 流水 actor_id 可伪造
现在：前端只带 token → 后端查库得到人 → 流水 actor_id = 真用户 id
```

**所以下一步：** 人认出来了，还要按 `role` 限制动作集合（6.5）。仅认人不够——inspector 照样能调 approve，只要状态合法。

---

### 6.5 `ACTION_ROLES`：角色表 + 执行顺序

**承接：** 6.4 已有 `current_user.role`。  
**缺什么：** `apply_gap_action` 仍只查 `TRANSITIONS`，不管角色。  
**所以这一步：** 增加 `ACTION_ROLES`，在转移表之前判一遍。

文件：`crud/projects.py`

```python
ACTION_ROLES = {
    "record": {"inspector"},
    "submit": {"inspector"},
    "reject": {"manager"},
    "approve": {"manager"},
    "close": {"manager"},
}
```

与产品约定对齐（机构内部两种人）：

| 角色 | 能做的动作 | 不能做的 |
|------|------------|----------|
| `inspector` 核查人员 | record、submit | reject / approve / close |
| `manager` 项目经理 | reject、approve、close | record / submit |

#### 执行流水线（在五章流水线上**插入一步**）

```text
1. 查 gap；没有 → 404
2. 【新】action 是否在 ACTION_ROLES，且 role 属于允许集合？
      否 → 403「当前角色无权执行该操作」
3. (status, action) 是否在 TRANSITIONS？
      否 → 400「当前状态不能执行…」
4. record/reject 的 comment 校验 → 400
5. 改 gap.status + 插 GapActionLog(actor_id=…)
6. commit / refresh
```

对应代码骨架：

```python
async def apply_gap_action(..., actor_id: int, role: str, comment: str | None = None):
    gap = await get_gap_by_id(db, gap_id)
    if not gap:
        raise HTTPException(status_code=404, detail="差距项不存在")

    allowed = ACTION_ROLES.get(action)
    if not allowed or role not in allowed:
        raise HTTPException(status_code=403, detail="当前角色无权执行该操作")

    key = (gap.status, action)
    if key not in TRANSITIONS:
        raise HTTPException(status_code=400, detail=f"当前状态「{gap.status}」不能执行「{action}」")
    # … 后面与五章相同：comment 校验、改状态、写 log
```

#### 写在这一步里要懂的：两张表各管什么

| 字典 | 键 | 值 | 回答 |
|------|----|----|------|
| `ACTION_ROLES` | `action` 字符串 | 允许的 `role` 集合 | **谁**能做这件事 |
| `TRANSITIONS` | `(当前状态, action)` | 下一状态 | **现在**能不能做、做到哪 |

不要把角色塞进 `TRANSITIONS` 的键里变成 `(状态, 动作, 角色)`——会爆炸式变复杂，且「无权」和「状态不对」分不清。

#### `/docs` 自测建议

1. 用 inspector 登录拿 token，对「待复核」POST `approve` → **403**（不是变成已通过）。  
2. 同一用户 POST `record`（状态若是待整改）→ 应按状态机走，成功则 200。  
3. 库里改/准备一个 `role=manager` 的用户，登录后对「待复核」`approve` → 200，状态变已通过。  
4. manager 对「待整改」`record` → **403**。  

种子数据：注册默认 `role=inspector`（`add_user`）；测经理需在库里把某用户 `role` 改成 `manager`，或另行插入。

**所以下一步：** 后端已硬拒绝；前端若仍五个按钮全亮，演示体验差且易误点 → 按 role 藏按钮（6.6）。记住：**藏按钮不是安全边界，403 才是。**

---

### 6.6 前端：按角色显按钮 + 请求体瘦身

**承接：** 6.5 后端已会 403。  
**缺什么：** 抽屉仍展示全部五个按钮；请求还可能带过时的 `actor_id`。  
**所以这一步：** 读 `localStorage.userInfo.role`；inspector 只显示代录/提交；manager 只显示退回/通过/关闭；Body 只留 `action` + `comment`。

文件：`views/project/gaps.vue`

```js
function getUserRole() {
  try {
    const info = JSON.parse(localStorage.getItem('userInfo') || '{}')
    return info.role || ''
  } catch {
    return ''
  }
}

const role = ref(getUserRole())
const canInspectorAction = () => role.value === 'inspector'
const canManagerAction = () => role.value === 'manager'
```

模板（示意）：

```vue
<el-button v-if="canInspectorAction()" ... @click="onAction('record')">代录说明</el-button>
<el-button v-if="canInspectorAction()" ... @click="onAction('submit')">提交复核</el-button>
<el-button v-if="canManagerAction()" ... @click="onAction('reject')">退回</el-button>
<el-button v-if="canManagerAction()" ... @click="onAction('approve')">通过</el-button>
<el-button v-if="canManagerAction()" ... @click="onAction('close')">关闭</el-button>
<p v-if="!role" class="role-tip">未读取到角色，请重新登录</p>
```

请求：

```js
await postGapAction(current.value.id, { action, comment })
// 不再传 actor_id
```

#### 写在这一步里要懂的

- `userInfo.role` 来自**登录**成功时后端返回的字段（第一章已存 localStorage）。改库里的 role 后，**必须重新登录**才会更新本地副本。经理开通新人（九）**不会**改当前浏览器里的 userInfo。  
- 前端 `v-if` = 体验与防呆；后端 403 = 真正安全。答辩要两句都说。  
- 失败提示继续读 `e.response?.data?.message` / `detail`，这样越权时页面能看见「无权」。

#### 联调做完标准（本章）

| 步骤 | 预期 |
|------|------|
| inspector 登录 → 开抽屉 | 只见代录、提交 |
| inspector 用 Network 强行 POST approve | 403，状态不变 |
| manager 登录 → 开抽屉 | 只见退回、通过、关闭 |
| manager 对合法「待复核」点通过 | 200，已通过，历史多一行，actor_id 是该经理 |
| 不带 Authorization 调 action | 401 |

**所以下一步（本段之外）：** 角色层够演示「谁能点什么」了；剧本里「李四看不见甲项目」要靠成员表过滤——见**第七章**。

---

### 6.7 本段做法浓缩 + 还没做的

**承接：** 6.2～6.6 贯通。  
**本段停在哪：** 改状态接口已强制登录；操作人来自 token；动作按 `role` 拒绝；抽屉按角色显按钮。  
**第六章故意后置、第七章已接上：** `project_member` 数据范围。  
**仍后置（已改口）：** 路由守卫 / 退出 / 录入 / 401 跳转见 **十三（已做）**；Agent 查库、Redis、RAG → 第二版。

1. **因果链：** 状态机通了 → 缺「谁」→ 带 token → 查 User → 用 `role` 管动作 → 前端藏按钮对齐演示。  
2. **三层各管一事（第六章）：** 401 没登录；403 角色不对；400 状态/说明不对。  
3. **`ACTION_ROLES` 与 `TRANSITIONS` 分表**，执行时先角色后转移（第七章再在角色前插入「是不是成员」）。  
4. **不要信 Body 里的 actor_id**；流水操作者 = `current_user.id`。  
5. **功能权限 ≠ 数据权限**；成员过滤见**第七章**。  
6. 测 manager：库里要有 `role='manager'` 的用户，并重新登录刷新 `userInfo`。

**代码落点总表（本章）：**

| 位置 | 作用 |
|------|------|
| `utils/request.js` | 请求拦截器写 `Authorization`；响应拦截器 401 清证跳登录（十三） |
| `utils/auth.py` → `get_current_user` | Header 剥 token → 调 crud → 注入当前用户 |
| `crud/user.py` → `get_user_by_token` | 被上一层调用：token → User |
| `schema/gap.py` | Body 仅 action、comment |
| `router/project.py` → `gap_action` | Depends + 传 id/role |
| `crud/projects.py` → `ACTION_ROLES` / `apply_gap_action` | 403 再 400 |
| `gaps.vue` | `v-if` 按 role；不再 post actor_id |

**下一阶段入口（写汇报/排期用）：**

```text
角色层已通
  → 已做（第七章）：项目成员表 → 「我的项目」列表过滤 → 改状态前校验是否成员
  → 已做（十三）：前端守卫、退出、录入、401 跳登录
  → 二版：对话三句查库…
```

---

## 七、角色之后：数据范围（项目成员过滤）

> 六章解决「核查人员不能点通过」。本章解决剧本里另一句：**李四看不见甲项目**——同样是 inspector，数据范围不同。  
> 按实际写代码顺序：7.1～7.7。

## 整条线怎么串起来（先看因果，再看分节）

```text
角色层已通（谁能点什么动作）
  → 但项目列表仍是全库项目；猜 URL 也能看别人的差距
  → 7.1 分清：功能权限 vs 数据权限；本章只做「人↔项目」多对多
  → 7.2 建 project_member 表 + ProjectMember 模型
  → 7.3 种子数据（SQL 插成员，先不做加人页）
  → 7.4 crud：list_project_ids / is_member / 改 getprojects·getlist_gaps·action·logs
  → 7.5 路由：list / list/gap / logs / action 全部 Depends(get_current_user) 并传 user_id
  → 7.6 双账号自测；前端通常不用改页（接口已过滤）
  → 7.7 本段收束；加人管理页 / 新建项目等后置
```

> **顺序提醒：** **新功能**一律 **router（可占位）→ crud**（见〇与登录章）。第七章是给**已有接口**补 `user_id` 过滤，正文按「先改 crud 签名 → 再改路由传参」叙述，属于改存量，不是推翻「router 优先」。

复习口诀（接六章）：

```text
token 认人
  → project_member 管「能看哪些项目的数据」
  → role 管「能点哪些动作」
  → TRANSITIONS 管「现在能不能流转」
```

拒绝顺序（action 上）：

```text
401 没登录
  → 403 不是该项目成员（数据范围）
  → 403 角色无权（功能权限）
  → 400 状态/说明不合法（状态机）
```

---

### 7.1 承接第六章：缺的是「数据范围」，不是再改按钮

**承接：** 六章 inspector/manager 按钮与接口已按角色分开。  
**演示还缺什么：** 李四登录后列表里不该出现甲；猜 `/project/甲id/gaps` 或直接调 `list/gap?project_id=甲` 也必须失败或空。  
**所以这一章：** 用成员表表达「谁属于哪个项目」，业务查询一律带当前用户过滤。

| 种类 | 靠什么 | 回答 |
|------|--------|------|
| 功能权限（六章） | `user.role` + `ACTION_ROLES` | 这种人能不能「通过」 |
| 数据权限（本章） | `project_member` | 这个人能不能动**这个项目**的数据 |

登录**不负责分配项目**；只认出 `user.id`。关系事先在成员表里，查列表/改状态时按 id 过滤。

**所以下一步：** 没有关系表就没法过滤 → 建表 + 模型（7.2）。

---

### 7.2 建表 + 模型：人和项目的多对多

**承接：** 7.1 定了要「人↔项目」关系。  
**缺什么：** 库里没有成员表。  
**所以这一步：** `project_member` + `ProjectMember`。

#### 表（MySQL）

不是在一行里塞「一堆成员 id」，而是**每一行一张「属于」小票**：

```sql
CREATE TABLE project_member (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL COMMENT '用户id',
  project_id INT NOT NULL COMMENT '项目id',
  CONSTRAINT for_user FOREIGN KEY (user_id) REFERENCES `user` (id),
  CONSTRAINT for_project FOREIGN KEY (project_id) REFERENCES project (id),
  UNIQUE KEY uk_user_project (user_id, project_id)
);
```

- `UNIQUE (user_id, project_id)`：同一个人不能重复进同一项目。  
- 自增 `id`：和其它表习惯一致；**业务唯一性靠唯一约束**，过滤查询主要用 `user_id` / `project_id`。  
- 若模型继承了 `Base.create_at`，建表时建议一并加 `create_at` 列，否则查询会报缺列。

#### 模型（`model/chat.py`）

```python
class ProjectMember(Base):
    __tablename__ = "project_member"
    __table_args__ = (
        UniqueConstraint("user_id", "project_id", name="uk_user_project"),
    )
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("user.id"), nullable=False
    )
    project_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("project.id"), nullable=False
    )
```

**所以下一步：** 表空着测不出「李四看不见甲」→ 插种子（7.3）。

---

### 7.3 种子数据（演示用 SQL，先不做加人页）

**承接：** 7.2 表已建。  
**缺什么：** 没有「张三∈甲、李四∉甲」的行。  
**所以这一步：** SQL 插成员；密码用户建议用经理「开通账号」（九）造，再改角色、挂成员。

示例（项目名改成你库里真实的，或改成数字 id）：

```sql
SELECT id, username, role FROM `user`;
SELECT id, name FROM project;

INSERT INTO project_member (user_id, project_id)
SELECT `user`.id, project.id
FROM `user`, project
WHERE `user`.username = 'zhangsan' AND project.name = '甲';

INSERT INTO project_member (user_id, project_id)
SELECT `user`.id, project.id
FROM `user`, project
WHERE `user`.username = 'jingli' AND project.name = '甲';

INSERT INTO project_member (user_id, project_id)
SELECT `user`.id, project.id
FROM `user`, project
WHERE `user`.username = 'lisi' AND project.name = '乙';
```

演示约定：

| 人 | 成员关系 | 登录后应看见 |
|----|----------|--------------|
| zhangsan（inspector） | ∈ 甲 | 只有甲 |
| jingli（manager） | ∈ 甲 | 只有甲，且能复核 |
| lisi（inspector） | ∈ 乙（或不进甲） | 没有甲 |

**所以下一步：** 有数据了，代码还没按成员过滤 → 写 crud（7.4）。

---

### 7.4 crud：小工具 + 改现有查询

**承接：** 7.3 种子已有。  
**缺什么：** `getprojects` 仍查全库；action 不校验成员。  
**所以这一步：** 成员小函数 + 改列表/差距/流水/动作。

文件：`crud/projects.py`

#### ① 小工具

```python
async def list_project_ids_for_user(db, user_id):
    # 返回 [1, 3, ...] —— 「我有哪些项目 id」
    stmt = select(ProjectMember.project_id).where(
        ProjectMember.user_id == user_id   # 必须是 列 == 参数，不能 user_id == user_id
    )
    ...

async def is_project_member(db, user_id, project_id) -> bool:
    # 我是不是这个项目的成员

async def ensure_gap_member(db, user_id, gap_id) -> GapItem:
    # 差距存在？→ 其 project_id 我是成员？否则 404/403
```

**坑：** `.where(user_id == user_id)` 永远为真，等于没过滤。必须 `ProjectMember.user_id == user_id`。

#### ② `getprojects(name, db, user_id)` —— 和路由参数对齐

路由会传 `user_id=current_user.id`，所以 crud **签名要多一个 `user_id`**：

```text
list_project_ids_for_user → 得到 ids
  → 若空：直接 []（不是成员则看不见任何项目）
  → select(Project).where(Project.id.in_(ids))，可再加 name LIKE
```

`list_project_ids_for_user` 只返回数字；**真正给前端的项目行仍由 `getprojects` 查 `project` 表**。两个函数职责不同，参数当然不一样。

#### ③ `getlist_gaps(project_id, db, user_id)`

- 传了 `project_id`：先 `is_project_member`，不是 → **403**；是 → 按项目筛差距。  
- 未传：只查 `project_id.in_(我的项目 ids)`。

#### ④ `apply_gap_action` / `list_gap_logs`

开头用 `ensure_gap_member(db, actor_id/user_id, gap_id)`：  
防「列表看不见，却拿别人的 gap_id 直接调 action/logs」。

**所以下一步：** crud 写好了，路由还要传 user_id → 改 router（7.5）。

---

### 7.5 路由：四个业务口都要认人 + 传 user_id

**承接：** 7.4 crud 已要 `user_id`。  
**缺什么：** 路由若不传，过滤不会生效；list/gap、logs 以前甚至没 `get_current_user`。  
**所以这一步：** 全部挂依赖并改调用。

文件：`router/project.py`

| 接口 | 改法 |
|------|------|
| `GET /list` | `Depends(get_current_user)` + `getprojects(..., user_id=current_user.id)` |
| `GET /list/gap` | 同上 + `getlist_gaps(..., user_id=...)` |
| `GET /gap/{id}/logs` | 同上 + `list_gap_logs(..., user_id=...)` |
| `POST /gap/{id}/action` | 原本已有用户；crud 内用 `actor_id` 做成员校验 |

示例（列表）：

```python
@router.get("/list")
async def getprojectlist(
    name: str | None = None,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    rows = await getprojects(name, db, user_id=current_user.id)
    ...
```

前端项目列表页**一般不用改**：仍调 `/api/project/list`，拦截器已带 token；后端返回的就是「我的项目」。

**所以下一步：** 双账号登录测通（7.6）。

---

### 7.6 联调做完标准

| 步骤 | 预期 |
|------|------|
| zhangsan 登录 → 项目列表 | 只有甲 |
| lisi 登录 → 项目列表 | 没有甲（有乙或空，取决于种子） |
| lisi 带 token 调 `list/gap?project_id=甲` | 403 |
| lisi 对甲下某 `gap_id` 调 action / logs | 403 |
| zhangsan 对甲下差距正常代录 | 仍受角色+状态机约束，成功则 200 |
| 不带 Authorization 调 `/list` | 401 |

---

### 7.7 补丁：经理看全部项目（`is_manager`）

**承接：** 7.4～7.6 做完后，所有人（含经理）都被成员表卡住——经理若不在 `project_member` 里，列表空、按钮全 403，复核演示走不通。  
**产品判断：** 核查员按成员过滤；**经理数据范围放开**（看全部项目/差距），动作仍受第六章 `ACTION_ROLES` 限制。

```python
def is_manager(role: str) -> bool:
    return role == "manager"
```

| 函数 | 经理行为 | 核查员行为 |
|------|----------|------------|
| `getprojects` | 不查成员表，返回全部（可按 name 搜） | 只返回成员表里的项目 |
| `getlist_gaps` | 不校验成员 | 非成员 → 403 |
| `ensure_gap_access` | 跳过成员校验 | 非成员 → 403 |

**易错：** 库里角色必须是字符串 `manager`（不是 `manage`）；改库后要**重新登录**刷新 `userInfo.role`。  
**边界：** 「能看全部」≠「能点所有按钮」——代录等仍看 `ACTION_ROLES`。

**做完标准：** 经理登录 → 项目列表有甲乙…；不在成员表也能进差距、能 approve（状态合法时）。

---

### 7.8 本段做法浓缩 + 还没做的

**承接：** 7.2～7.7 贯通。  
**本段停在哪：** 核查员按成员过滤；经理数据范围放开；与角色层叠在一起。  
**故意还没做：** 「加人」管理页与新建写成员——**十一已补**。  
**第一版收口（已落地，见十三）：** 退出登录、录入差距、路由守卫、axios 401 跳登录。跨项目汇总没意义，不做。对话查库仍第二版。

1. **赋予 = 成员表插行；过滤 = 业务查询带 user_id / role。** 登录不分配项目。  
2. **多对多 = 多行关系，不是一个字段塞集合。**  
3. **`list_project_ids_for_user` ≠ `getprojects`**：前者 id 列表，后者项目实体。  
4. **where 必须 `Model.列 == 参数`。**  
5. **列表过滤不够**：action/logs 也要 `ensure_gap_access`，防猜 id。  
6. **四层拒绝：** 401 → 403 成员（经理跳过）→ 403 角色 → 400 状态。  
7. **经理全量是数据范围例外，不是取消角色表。**

**代码落点总表（本章）：**

| 位置 | 作用 |
|------|------|
| MySQL `project_member` | 人↔项目 |
| `model/chat.py` → `ProjectMember` | 模型 |
| `crud/projects.py` → `is_manager` / `list_project_ids_for_user` / `is_project_member` / `ensure_gap_access` | 成员 + 经理例外 |
| `getprojects` / `getlist_gaps` | 列表过滤（经理全量） |
| `apply_gap_action` / `list_gap_logs` | 改状态/看历史前校验 |
| `router/project.py` | 四个接口传 `current_user.id` + `role` |

**下一章入口（第一版后续）：** Layout 导航 → 经理开通账号。

---

## 八、Layout：嵌套路由 + 侧栏导航

> 七章之后业务页都能进，但项目页、差距页、聊天页仍是「各自一整屏」。第一版要收成：**侧栏常驻 + 中间换页**。

## 整条线怎么串起来

```text
业务页已通，但没有统一壳
  → 8.1 父级 Layout（侧栏 + 中间 router-view）
  → 8.2 路由改成 / + children；/login 平级单独
  → 8.3 侧栏用 router-link；带 id 的跳转仍用 push（见 5.4）
  → 8.4 白屏坑：父级必须是 Layout、Login 要 import、子路径用 @/
```

### 8.1 这一步在解决什么

| 已有 | 还缺 |
|------|------|
| `/project`、`/chat`、`/project/:id/gaps` 各是独立页 | 公共侧栏（我的项目 / 智能助手） |
| `App.vue` 一层 `<router-view />` | 再套一层：外壳固定、中间换内容 |

**Layout 干什么：** 左边菜单常在，右边根据 URL 换子组件。  
**不是：** 把所有页面 import 进 Layout 用 `v-if` 切换——那是路由该干的事。

### 8.2 嵌套路由怎么配

`router/index.js` 结构：

```js
{ path: '/login', component: () => import('@/views/Login.vue') },  // 平级，无侧栏
{
  path: '/',
  component: () => import('@/views/Layout.vue'),  // 父级 = 壳
  redirect: '/project',
  children: [
    { path: 'project', component: () => import('@/views/project/project.vue') },
    { path: 'project/:id/gaps', component: () => import('@/views/project/gaps.vue') },
    { path: 'chat', component: () => import('@/views/Chat.vue') },
    { path: 'users/create', component: () => import('@/views/user/CreateUser.vue') },
  ],
},
```

渲染链：

```text
App.vue 的 router-view
  → 匹配到 Layout（路径 / 下）
    → Layout 里再一个 router-view
      → 匹配到 children 里的某一页
```

**知识点：`<router-view>` 是「当前匹配组件的坑位」。**  
点 `<router-link>` 改的是 URL；Router 按路由表匹配；对应组件塞进对应层级的 `router-view`。链接不「找」view，view 只负责显示。

| 层级 | 文件 | 出什么 |
|------|------|--------|
| 外层 | `App.vue` | Login **或** Layout |
| 内层 | `Layout.vue` | project / gaps / chat / CreateUser |

### 8.3 侧栏：`router-link`（对照 5.4）

```vue
<el-aside>
  <router-link to="/project">我的项目</router-link>
  <router-link to="/chat">智能助手</router-link>
</el-aside>
<el-main>
  <router-view />
</el-main>
```

| 场景 | 用什么 |
|------|--------|
| 侧栏固定地址 | `router-link` |
| 登录成功、项目行拼 `id` | `router.push` |

### 8.4 白屏常见坑（第一版踩过）

| 现象 | 原因 |
|------|------|
| 整站白屏 | `{ component: Login }` 却没 import / 没懒加载 |
| 有壳无内容 | 父级误写成 `Login.vue`，登录页没有子级 `router-view` |
| Vite 报 Failed to resolve | 写成 `.../Chat.vue`，必须 `@/views/...` |
| 进 `/` 空 | 缺 `redirect: '/project'` |

**做完标准：** 登录后进 `/project`，左侧有链接，点「智能助手」中间换成 Chat，侧栏还在。

---

## 九、经理开通账号（系统内的「注册」）

> **放在这里的原因：** 开通账号是进系统之后、Layout 侧栏里的功能，不是登录页的一部分。  
> **第一章**只解决「怎么登录」；本章解决「经理怎么给别人建号」。

## 整条线怎么串起来

```text
已有 Layout + 经理角色
  → 登录页已去掉公开注册
  → 9.1 后端 /register：Depends(get_current_user) + 仅 manager；不发新人 token
  → 9.2 复用 add_user / post_userinfo（二章 crud）
  → 9.3 前端 CreateUser.vue + 侧栏仅经理可见
  → 9.4 成功只提示清空表单，不改经理 localStorage
```

### 9.1 后端：谁能调、返回什么

`router/loginuser.py` 现逻辑要点：

1. `current_user: User = Depends(get_current_user)` —— 没 token → 401  
2. `current_user.role != "manager"` → 403「仅经理可开通账号」  
3. 查重 → `add_user`（默认 `role=inspector`）  
4. **不调** `post_token`；data 里只有新用户的 `userInfo`

| 与早期公开注册对比 | 旧 | 现（第一版） |
|--------------------|----|--------------|
| 谁可调 | 任何人 | 已登录经理 |
| 返回 token | 有（新人） | **无** |
| 登录页入口 | 有注册 | **无** |

**`Depends(get_current_user)` 有啥用：** 安全性——先认人，再判是不是经理，禁止匿名建号。

### 9.2 前端页（挂在项目区 Layout 里）

- 路由：`users/create` → `views/user/CreateUser.vue`（第八章 children）  
- 表单：账号 / 密码 / 工号；调 `post_register`（拦截器带经理 Bearer）  
- 成功：提示 + `clearForm`；**不要**写 localStorage；**不要** `push` 换人  

`CreateUser.vue` 核心（与登录页的区别：多工号、不开会话）：

```js
import { post_register } from '@/api/login'

async function onCreate() {
  // 空值校验账号/密码/工号 …
  const res = await post_register({
    username: form.value.username,
    password: form.value.password,
    employee_no: form.value.employee_no,
  })
  ElMessage.success(res.data?.message || '开通成功')
  clearForm()   // 不写 token / userInfo
}
```

`Layout.vue` 侧栏仅经理可见：

```js
const isManager = computed(() => {
  const info = JSON.parse(localStorage.getItem('userInfo') || '{}')
  return info.role === 'manager'
})
```

`v-if="isManager"` →「开通账号」→ `/users/create`。
### 9.3 联调做完标准

| 步骤 | 预期 |
|------|------|
| 核查员侧栏 | 没有「开通账号」 |
| 经理侧栏 | 有，进 `/users/create` |
| 经理开通新用户 | 200，经理仍是自己（token 不变） |
| 不带 token 调 `/register` | 401 |
| inspector 强行调 `/register` | 403 |
| 登录页 | 只有登录，无注册切换 |

### 9.4 本段浓缩

1. **开通 ≠ 自助注册：** 操作人是经理，新建的是别人。  
2. **绝不能给新用户发 token 并写进当前浏览器**，否则经理会话被顶掉。  
3. **前端藏入口不够**，后端必须 `Depends` + role 校验。  
4. 新人默认 `inspector`；要经理账号仍用种子 SQL 改 `role`（或第二版加角色下拉）。  
5. **开通页校验加固（十四）：** 前端 TS + `rules` 与后端 `UserRequest` 的 `Field` 对齐；登录 schema 不加严，种子账号可留。

---

## 十、第一版总复习清单（答辩/自测）

### 10.1 主路径（应能口述 + 亲手走通）

```text
登录（只登录；回车可提交，见十三.4）
  → Layout：侧栏「我的项目」；顶栏可退出
  → 项目列表（核查员只见成员项目；经理见全部）
  → 点进差距列表 → 抽屉
  → 代录 / 提交 / 退回 / 通过 / 关闭（角色 + 状态机）
  → 历史能翻；单项目状态统计能看
  → 经理：开通账号 / 新建项目 / 分配核查（加人、悬停删人）
  → 录入差距（差距页弹窗 CreateGapDialog）
  → 「智能助手」仍是最小聊天（未查业务库）
```

### 10.2 四层拒绝（背熟）

| 顺序 | 码 | 含义 |
|------|-----|------|
| 1 | 401 | 没登录 / token 无效 |
| 2 | 403 | 非成员（经理数据范围跳过） |
| 3 | 403 | 角色无权做该 action / 非经理开通·分配·移除 |
| 4 | 400 | 状态不允许或说明缺失 / 成员已存在等 |

### 10.3 表（第一版）

`user`、`user_token`、`project`、`project_member`、`gap_item`、`gap_action_log`、（聊天相关表保留）

### 10.4 第一版已补强 / 收口状态 / 第二版

**十一章已做完：** 新建项目、分配成员、移除成员。  
**差距页已有：** 单项目各状态数量统计。  
**十三章已做完（原「第一版还差」）：** 退出登录、录入差距、路由守卫、axios 401 跳登录；登录回车提交也收口。

**第一版主线 + 收口：可视为齐。** 可选硬化（非阻塞答辩）：建项目后端限经理、抽屉按钮按状态灰显、聊天挂鉴权等。

单项目状态统计已有；业务按项目看进度即可，**跨项目汇总没意义，不做**。

**第二版仍后置：** Agent 工具查库三句；Redis；RAG；开通账号时角色下拉等。

### 10.5 复习用法

- **先看〇开局配置：** 请求/响应拦截器 + `get_current_user` 写一次、接口反复挂。  
- **正推：** 一→十三。  
- **巩固建议：** 口述新建为何要写成员表；扫一遍 **11.6 踩坑表**；口述 **十三**「守卫 vs 401 拦截器」差在哪、为何地址栏打开 `/api/...` 不会跳登录。

---

## 十一、新建项目 / 分配与移除成员（提问 + 踩坑详记）

> 按「自己先想流程 → 对照缺陷补全 → 写代码 → 问清楚易混点」记。  
> 认人模板不重写，见 **〇**；`commit`/`refresh` 见 **〇.5**。

## 整条线怎么串起来

```text
管理侧缺：建项目、给人进项目、把人移出
  → 11.1 先定流程（自己的话）+ 补上的缺陷（路径、字段、成员表、token）
  → 11.2 新建项目：crud 写 project + 创建人进 member；路由 Depends
  → 11.3 分配页：选项目 → 拉成员 → 输 user_id 加人 → 再拉成员
  → 11.4 删人：悬停 × → 确认 → DELETE → 再拉成员
  → 11.5 提问整理（经理全量、核查员能否建、id 从哪来…）
  → 11.6 踩坑清单（明天复习优先看）
  → 11.7 浓缩 + 代码落点 + 31 日前目标
```

---

### 11.1 自己定的流程 + 必须补上的缺陷

#### 自己一开始怎么想的（保留原话逻辑）

**新建项目：**

```text
侧栏新页（Layout children）
  → 表单填项目信息 → POST 请求体
  → router 收 body → crud add 进 project
  → 成功后再拉 /list → 列表重渲染
```

**分配/删除成员：**

```text
字段：user_id + project_id → 只动 project_member
前端已选项目有 projectId；加人用手填 userId；删人要点行上的人
后端：接口 + Depends；crud 插/删一行；前端刷新成员区
```

#### 整合后的完整流程（答辩可直接说）

**新建：**

1. `POST /api/project/create`（挂在现有 `/api/project` 下，不要另起 `/api/new_createproject`）  
2. `Depends(get_current_user)`——要创建人 id，靠 token，不信 Body 自报  
3. Body 只收 `name`、`remark`（**不要 id**）  
4. crud：`Project` 入库 → `refresh` 拿 id → 再写 `ProjectMember(创建人)`  
5. 前端新页 + 子路由 + 侧栏；成功跳回「我的项目」再拉 list  

**加人 / 删人：**

1. `POST /api/project/member`、`DELETE /api/project/member`（Body：`user_id`、`project_id`）  
2. 仅 `manager`；`GET /api/project/alluser?project_id=` 刷列表  
3. 成功后前端再 `loadMembers`，删接口**不必**返回整份成员表  

---

### 11.2 新建项目：实施要点

#### schema

`ProjectRequest`：`name: str`，`remark: str | None`。

#### 路由（先写；crud 用注释占位）

```python
@router.post("/create")  # 最终路径 /api/project/create
async def post_cre_project(
    body: ProjectRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    # res = await post_create_project(...)  # crud 写完再取消注释
    res = await post_create_project(
        body.name, body.remark, db, current_user.id
    )
    ...
```

**谁能建：** 「仅经理」在**路由**里加 `role != manager → 403`（产品选择）。当前实现以登录用户为创建人写成员即可。

#### crud（后写；对照 `add_user`，用模型不是字典）

```python
row = Project(name=name, remark=remark)
db.add(row)
await db.commit()
await db.refresh(row)   # 同步 id 等库生成字段

member = ProjectMember(user_id=current_user_id, project_id=row.id)
db.add(member)
await db.commit()
```

#### 前端

- 路由：`project/create` → `create_project.vue`（**不要**误指到 `project.vue`）  
- 表单：`name` / `remark` → `post_add_project` → 成功 `router.push('/project')`  

---

### 11.3 分配成员页

#### `projectId` / `userId` 从哪来？（当时的疑问）

```js
const projectId = ref(null)  // 先声明空盒子
const userId = ref(null)
```

| 变量 | 怎么有值 |
|------|----------|
| `projectId` | `<el-select v-model="projectId">`，选项 `:value="item.id"`，选中后 = 项目 id |
| `userId` | `<el-input v-model="userId">`，**加人时手填**要拉进项目的用户 id |
| `m.user_id` | `alluser` 返回的每一行；**删人时用这个**，不是输入框 |

`projects` 来自 `getprojects`；选项目后 `watch(projectId)` → `loadMembers` → `get_alluser`。

#### 加人成功后为何要再拉成员

和「即时更新已有成员」同一套路：`post_add_member` 成功 → `await loadMembers(projectId)`。  
不必让加人接口返回整表；复用 `alluser` 即可。

---

### 11.4 移除成员

```text
悬停成员行 → 显示 × → ElMessageBox.confirm
  → delete_member({ user_id: m.user_id, project_id: projectId })
  → 成功 → loadMembers
```

- 只删 `project_member` 一行，**不删** `user` / `project`。  
- 被移出的核查员再进「我的项目」：第七章过滤自动生效，**不用再写一套删除刷新项目列表**。  
- 经理侧：变的是「已有成员」区域，不是整个项目列表消失。  

后端：`remove_project_member` + `DELETE /api/project/member` + 仅经理。

---

### 11.5 提问整理（当时问过的，复习用问答）

**Q1：创建人 id 是不是还要靠 token？**  
要。成员表要 `user_id`，用 `Depends(get_current_user)` → `current_user.id`，不要前端 Body 自报。

**Q2：token→id 这套是不是每次新建功能都要重写？**  
不用。`get_current_user` 写一次；每个新接口只挂 `Depends`。

**Q3：写 `ProjectMember` 是不是把创建者归为成员？经理不是能看全部吗？当时是不是给经理在每个项目都加了一遍？**  
- 是：创建者进成员表。  
- 经理看全部靠 `is_manager(role)` **跳过成员过滤**，不是给经理插满成员表。  
- 两种机制别混。

**Q4：那核查员也能建项目吗？**  
看路由是否校验 `manager`。crud 不拦的话谁登录成功谁就是创建人成员；产品上常见仅经理可建。

**Q5：`await db.refresh(row)` 是不是入库并拿到 id？**  
差点：`commit` 才入库；`refresh` 是再从库读到内存 `row`（id、默认时间）。习惯：`commit → refresh → 用 row.id`。

**Q6：`row = Project(name=..., remark=...)` 有没有别的形式？**  
有（空对象再赋值、`**dict`），本项目统一用关键字参数，和 `add_user` 一致。`row = {"name": name}` **不行**，那是字典不是 ORM。

**Q7：分配页 `projectId`/`userId` 从哪里获得？**  
见 11.3：v-model；删人用行内 `m.user_id`。

**Q8：成员列表「还是没有即时更新」？**  
方向应是选项目/加人成功后调 `alluser`。若仍空：看 Network 里 `alluser` 的 `data`；注意别在 `.vue` 里重复 `export const get_alluser`（曾导致编译失败）。后端日志若已 200 且 SQL 有 join，重点查前端赋值与路由是否指对页面。

**Q9：第一版还差什么？31 前一二版写完有可能吗？**  
- 原「还差退出、录入」已落地（十三）；主线 + 收口可演示。  
- 31 前：可选硬化（建项目后端限经理等）；Redis 一处有可能；Agent 三句 + RAG 偏紧。

---

### 11.6 踩坑清单（自己犯过 / 差点犯的）

| 坑 | 现象 | 正确做法 |
|----|------|----------|
| `row = {"name": name}` 或 `project.name=name` 写在 `{}` 里 | 不能 `db.add` 进表 | `row = Project(name=..., remark=...)` |
| 参数名 `currenuser_id`，下面写 `current_user_id` | 爆红：is not defined | 名字拼写一致 |
| 调用里写 `body.name:str` | 语法错：类型注解不能写在调用实参上 | `body.name`；类型只写在函数定义 |
| 调 crud 参数顺序乱 / 路径写成 `/api/project/create` 又挂在已有 prefix 上 | 404 或参数错位 | `@router.post("/create")`；关键字传参更稳 |
| 路由 `project/create` 的 component 仍指向 `project.vue` | 点新建还是列表页 | 指到 `create_project.vue` |
| 创建表单两个 input 塞同一个 `el-form-item` | 结构别扭、难绑 v-model | 拆成两个 form-item（对照开通账号页） |
| 只加 project 不写 member | 核查员建完列表看不见 | 创建人写 `project_member` |
| 以为经理全量 = 成员表插遍 | 概念混 | 经理靠 `is_manager` 跳过过滤 |
| 删人还去「点一下拿项目 id」 | 多余 | 已有 `projectId`；人用 `m.user_id` |
| `.vue` 里又 `export const get_alluser` | Vite：Identifier already been declared | api 只放 `projects.js`，页面只 import |
| `post_login` 行爆红 “not awaitable” | 多为类型检查误报 | `async def` + `await` 运行一般正常；悬停看具体信息 |

---

### 11.7 本段浓缩 + 代码落点

1. **建项目 = 项目行 + 创建人成员行；** id 靠 commit/refresh。  
2. **加人/删人 = 只动成员表；** 仅经理 + token。  
3. **前端刷新成员 = 再调 alluser**（加人、删人后同一套路）。  
4. **认人复用〇，不重写 auth。**  
5. **经理全量 ≠ 成员表插满。**

| 位置 | 作用 |
|------|------|
| `schema/info.py` → `ProjectRequest` / `ProjectMemberRequest` | 请求体 |
| `crud/projects.py` → `post_create_project` / `add_project_member` / `remove_project_member` / `get_all_user` | 写删查 |
| `router/project.py` → `/create` `/member` `/alluser` | Depends；分配/移除校验 manager |
| `api/projects.js` | `post_add_project` / `post_add_member` / `get_alluser` / `delete_member` |
| `create_project.vue` | 新建表单 |
| `devide_member.vue` | 选项目、成员列表、加人、悬停删除 |
| `Layout.vue` | 经理侧栏：新建项目、分配核查 |

**明天复习最小清单：**

1. 口述 11.1 整合流程一遍。  
2. 默写：`add → commit → refresh → 写成员`。  
3. 分清：`projectId` / 输入框 `userId` / 行内 `m.user_id`。  
4. 扫一遍 11.6 踩坑表。  
5. 亲自走：新建 → 加人 → 删人 → 换核查员看项目是否消失。

---

## 十二、前端美化：方案 A 等保工作台

> 目标：看起来像**机构内部系统**，不是营销落地页。功能逻辑不动，只改壳与卡片。

### 视觉约定

| 区域 | 做法 |
|------|------|
| 侧栏 | 深色 `#15202b`，品牌「等保整改跟踪」，菜单高亮左侧青绿条 |
| 顶栏 | 白底；显示用户名 / 角色；**退出**清 token 回登录 |
| 主区 | 浅灰底 `#e8edf3`；业务内容用白卡片 `.page-panel` |
| 主色 | 青绿 `#1d6b5c`（覆盖 Element Plus `--el-color-primary`） |
| 登录 | 同色系居中卡 + 轻氛围背景 |

### 代码落点

| 文件 | 作用 |
|------|------|
| `src/styles/theme.css` | CSS 变量 + `.page-panel` |
| `main.js` | 引入 theme.css |
| `Layout.vue` | 工作台壳（侧栏 + 顶栏 + 退出） |
| `Login.vue` | 登录卡 |
| 各业务页 | 去掉黑框，改用 `page-panel` |

### 验收

登录后应看到：左侧深色菜单、顶部身份、中间白卡片列表；点退出回到登录页。

---

## 十三、第一版收口落地 + 答疑（退出 / 录入 / 守卫 / 401 / 登录回车）

> 文档 §10 曾写「还差退出、录入」——**代码已落地**。本章记：做了什么、和开局拦截器怎么叠、以及自测时踩过的认知坑。

## 整条线怎么串起来

```text
业务主线已通（登录→项目→差距→状态机→管理侧→美化）
  → 缺：主动退出、录入新差距、没 token 进不了业务页、token 失效时自动踢回登录
  → 13.1 退出 + 录入差距
  → 13.2 路由守卫（进页前看有没有 token）
  → 13.3 axios 响应拦截器（接口 401 清证跳登录）+ 和守卫的分工
  → 13.4 登录回车：浏览器 form submit + @submit.prevent + native-type="submit"
  → 13.5 提问整理 / 踩坑
  → 13.6 浓缩 + 代码落点
```

---

### 13.1 退出登录 + 录入差距

#### 退出

`Layout.vue` 顶栏「退出」：

```js
function logout() {
  localStorage.removeItem('token')
  localStorage.removeItem('userInfo')
  router.push('/login')
}
```

与登录对称：登录 `setItem`，退出 `removeItem`。必须两项都清，否则顶栏还能显示旧 `userInfo`。

#### 录入差距

| 位置 | 作用 |
|------|------|
| `POST /api/project/gap/create` | `Depends(get_current_user)`；crud `create_gaps`（成员/经理校验、标题非空） |
| `api/projects.js` → `post_create_gap` | 前端封装 |
| `CreateGapDialog.vue` | 弹窗表单；成功后 `emit('success')` |
| `gaps.vue` | 「录入差距」打开弹窗；成功后刷新列表/统计 |

MVP **不做**独立 `/gaps/:gapId` 详情页；列表 + 抽屉 + 录入弹窗即可。

---

### 13.2 路由守卫（进页面）

`router/index.js`：

```js
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.path === '/login') {
    if (token) return next('/project') // 已登录别再进登录页
    return next()
  }
  if (!token) return next('/login') // 没证进业务页 → 踢回登录
  next()
})
```

**只认「有没有 token 字符串」，不验是否过期/有效。**  
有假 token 仍能进 `/project`，列表请求才会 401——这时要靠 **13.3**。

自测守卫：Application → Local Storage → **删掉** `token` → 打开 `/project` → 应去 `/login`。

---

### 13.3 响应拦截器 401 → 登录（进页后的接口失败）

#### 正确写法

```js
// utils/request.js 响应拦截器 error 分支
if (error.response?.status === 401) {
  localStorage.removeItem('token')
  localStorage.removeItem('userInfo')
  location.href = '/login'
}
return Promise.reject(error)
```

- 看 **`error.response?.status === 401`**：后端 `HTTPException(401)` 经异常处理器仍是 **HTTP 状态 401**（body 里也有 `"code":401`，但 axios 失败分支用的是 HTTP status）。  
- 清证必须用 `localStorage.removeItem(...)`，**不能**写 `token=""`（作用域里没有这个变量，严格模式会 `ReferenceError`，可能拦在 `location.href` 前面导致**跳不过去**）。  
- `location.href` 整页刷新，第一版够用；要用 `router.push` 需注意别和 `request.js` 循环引用。

#### 两套「跳登录」各管什么（答辩必分清）

| 机制 | 管什么 | 不管什么 |
|------|--------|----------|
| `beforeEach` | 进 `/project`、`/chat` 等 **Vue 页面**时有没有 token | 不校验 token 真假；不管 API |
| axios 响应拦截器 | 页面里用 `request` 调接口收到 **HTTP 401** | 地址栏直接打开 `/api/...` 时浏览器只显示 JSON，**不会**跑 Vue/axios |

#### 现象对照（今天踩过）

| 你做的操作 | 看到什么 | 原因 |
|------------|----------|------|
| 地址栏打开 `http://localhost:5173/api/project/list` | 一页 JSON `code:401` | Vite 代理到后端；没有 Vue Router、没有 axios |
| 打开 `/project`，有坏/过期 token，拦截器未写好 | 侧栏在、「加载失败」、不跳登录 | 守卫放行（有字符串）；`project.vue` 只 `ElMessage.error`；401 跳转未接或写错 |
| 删光 token 再开 `/project` | 去 `/login` | 守卫生效 |

**业务页 catch 里的「加载失败」≠ 跳登录。** 跳登录应集中在响应拦截器（或守卫），不要指望每个页面自己写。

---

### 13.4 登录回车：用浏览器表单 submit（规范做法）

#### 要达成的体验

密码框（以及账号框）按 **Enter**，效果等同点「登录」。

#### 推荐写法（已落到 `Login.vue`）

```vue
<el-form @submit.prevent="onLogin">
  <!-- 账号、密码 input -->
  <el-button type="primary" native-type="submit" :loading="loading">
    登录
  </el-button>
</el-form>
```

| 片段 | 含义 |
|------|------|
| 浏览器内置 | 点 `type="submit"` 按钮，或单行 input 里按 Enter → 触发表单 **`submit` 事件**（平台机制，不是你手写的 if） |
| `native-type="submit"` | Element Plus 按钮默认未必是原生 submit；写成 submit 才参与上面机制 |
| `@submit` | 接到浏览器发出的 submit |
| `.prevent` | `preventDefault`，拦住默认「整页提交/刷新」 |
| `="onLogin"` | 改跑我们的登录逻辑 |

**一句话：** 条件满足 → 浏览器发 submit → `@submit.prevent` 接到 → 只调 `onLogin`。

#### 为何不优先只写 `@keyup.enter="onLogin"`

能用，但不如 form submit 规范：

1. **覆盖：** submit 时账号/密码框回车都能登；只绑密码则账号框回车没反应。  
2. **一条路径：** 回车与点按钮都走 `@submit` → `onLogin`；keyup 是第二条点击之外的第二条线。  
3. **语义 / 无障碍：** `<form>` + submit 更符合 HTML；读屏、密码管理更认表单提交。

#### 和「谁在判断触发条件」

触发条件**不是 Vue 里写的**，是浏览器对 `<form>` 的默认行为。你没有写 `if (按了回车)`；你写的是：**一旦发生 submit，就跑 onLogin。**

没写 `@submit` 时，回车/点提交仍可能触发表单默认提交，只是**不会**跑你的登录逻辑（容易刷新或怪跳）。

---

### 13.5 提问整理（今天）

**Q1：接口 401 了为什么没自动去 login？**（地址栏打开 `/api/project/list`）  
那是 API，不是页面。浏览器只渲染 JSON；守卫和 axios 都不会跑。

**Q2：已经在 `/project` 了，加载失败也不跳？**  
多半 localStorage 里**还有** token，守卫放行；接口 401 后若拦截器未写好，页面只 toast「加载失败」。

**Q3：`if (401) { token=""; location.href='/login' }` 对吗？**  
方向对，`token=""` **错**。应 `localStorage.removeItem('token'|'userInfo')`，否则可能 ReferenceError 导致跳不过去。

**Q4：`@submit.prevent="onLogin"` 是什么意思？**  
监听表单 submit → 阻止默认刷新 → 调 onLogin。配合 `native-type="submit"` 让回车/点按钮都走进这条路。

**Q5：这是浏览器的监听机制吗？满足就自动触发 onLogin？**  
接近：浏览器内置「何时 submit」；你用 `@submit` 挂上之后才会进 onLogin。不是浏览器直接认识 `onLogin` 这个函数名。

**Q6：第一版现在还差什么？**  
主线 + 收口 + 开通校验（十四）已齐。Redis（见 `缓存点.md`）与 Agent 查库（见 **十五**）已接。剩余可选：退出删 Redis token、抽屉按状态灰显、RAG（明确不做也可口述理由）。

---

### 13.6 本段浓缩 + 代码落点

1. **退出 = 清 token + userInfo → `/login`。**  
2. **录入 = `gap/create` + 弹窗；成功刷新列表。**  
3. **守卫管进页；401 拦截器管接口失效。** 地址栏敲 `/api` 永远只见 JSON。  
4. **清证用 `removeItem`，不要 `token=""`。**  
5. **登录回车 = form submit 机制 + `@submit.prevent` + `native-type="submit"`。**

| 位置 | 作用 |
|------|------|
| `Layout.vue` | `logout()` |
| `router/index.js` | `beforeEach` 守卫 |
| `utils/request.js` | 请求带证 + 响应 401 跳登录 |
| `CreateGapDialog.vue` / `gaps.vue` | 录入 UI |
| `router/project.py` → `/gap/create` | 录入接口 |
| `crud/projects.py` → `create_gaps` | 写入 `gap_item` |
| `Login.vue` | `@submit.prevent` + `native-type="submit"` |

**验收最小清单：**

1. 登录 → 顶栏退出 → 回登录页；再进 `/project` 应被守卫拦住。  
2. 登录后改坏 token → 刷新业务页 → 应 401 并跳登录。  
3. 差距页录入一条 → 列表出现。  
4. 登录页密码框回车 → 触发登录（与点按钮相同）。

---

## 十四、开通账号：TS 类型 + el-form 规则 + 后端 Pydantic（能讲清即可）

> 落在「经理开通账号」页（`CreateUser.vue`）与 `schema/loginuser.py` 的 `UserRequest`。  
> **强规则只约束开通，不约束登录** → 库里旧种子用户可留，不必先改哈希。

## 整条线怎么串起来

```text
开通要「前后端同一套长度规则」
  → 后端 UserRequest：Field(min_length / max_length) —— 最终裁决
  → 前端 CreateUser：interface 形状 + rules 运行时校验 + formRef.validate
  → 登录 loginRequest：保持 username/password 裸 str —— 种子账号能登
  → （可选）页面 lang="ts"；全仓迁 TS 非本段目标
```

---

### 14.1 四件套各干什么（别混）

| 东西 | 干什么 | 不是什么 |
|------|--------|----------|
| `form` | 真正的数据（`v-model` 绑它） | 不是校验器 |
| `formRef` + `FormInstance` | 拿到 **el-form 组件实例**，才能调 `validate()` / `clearValidate()` | 不存业务字段值 |
| `rules` | **运行时**校验（必填、min/max、提示文案） | 不是 TS 类型 |
| `interface RegisterForm` | **编译期**说明书：`form` 有哪些字段、各是什么类型 | **不是**校验；编译后消失 |

模板绑定关系：

```text
el-form
  ref="formRef"           → 挂到 formRef
  :model="form"           → 数据在这
  :rules="rules"          → 规则在这
el-form-item prop="xxx"   → 这条规则对应 form.xxx（三处同名）
```

提交：`formRef.value?.validate()` 通过后再 `post_register`。

一句话记忆：

> **formRef = 调校验；rules = 校验规则；form = 数据；interface = 给 form 的类型说明书，不是校验本身，但上了 TS 就值得写。**

---

### 14.2 `FormRules<RegisterForm>` 到底约束什么

```ts
const rules: FormRules<RegisterForm> = {
  username: [ { required: true, min: 3, max: 32, message: '...' }, ... ],
  password: [ ... ],
  employee_no: [ ... ],
}
```

**`FormRules<RegisterForm>` 表示：`rules` 里的字段名，以 `RegisterForm` 为准**（`username` / `password` / `employee_no`）。

- 字段名写错（如 `userName`）→ TS 可能报错。  
- **是「字段名由它约束」，不是「规则内容由它生成」。**  
- 每个字段下面的 `required`、`min`、`message` **仍要自己写**（对照后端 `Field` 数字）。

`FormRules` / `FormInstance` 都是 **Element Plus 官方类型**，从 `element-plus` import，不是自己发明的关键字。

---

### 14.3 后端怎么对齐

```python
# schema/loginuser.py
class UserRequest(BaseModel):
    username: str = Field(min_length=3, max_length=32)
    password: str = Field(min_length=6, max_length=64)
    employee_no: str = Field(min_length=1, max_length=32)

class loginRequest(BaseModel):
    username: str   # 登录不加严
    password: str
```

- 空的 `Field()` ≈ 没加规则；约束要写进参数里。  
- 不合规开通请求 → 通常 **422**，进不了 `add_user`。  
- **以后端为准**；前端 rules 是体验，防绕过靠后端。

---

### 14.4 对象字面量里不能写 `username: string = ''`

```ts
// 变量可以：
let username: string = ''

// 对象里只能写值；类型在 interface / 泛型上：
const form = ref<RegisterForm>({
  username: '',   // 对
  // username: string = ''  // 错：对象的 : 是「属性:值」
})
```

`catch (e)` 里 `e` 是 `unknown`，不能直接 `e.response`；用 `axios.isAxiosError(e)` 再取 message。

---

### 14.5 登录页要不要同一套？

| | 开通（CreateUser） | 登录（Login） |
|--|-------------------|---------------|
| 强长度 rules | 要（与 UserRequest 对齐） | 通常只要非空 |
| `formRef` + FormRules | 要 | 可选 |
| `interface` + `ref<T>` | 建议有 | 可有（只要形状） |
| 旧种子账号 | 不经过开通规则 | **仍能登**（loginRequest 不加严） |

---

### 14.6 口述要点（30 秒）

1. TS `interface` 只描述字段类型，不写长度。  
2. `el-form` `rules` + `formRef.validate()` 做运行时校验，数字和后端一致。  
3. `FormRules<RegisterForm>` 约束规则对象的 **key**；`min`/`message` 手写。  
4. 后端 `Field` 是最终裁决。  
5. 强规则只挂开通，登录放松 → 种子用户可留。

---

### 14.7 代码落点

| 位置 | 作用 |
|------|------|
| `back/schema/loginuser.py` → `UserRequest` | Pydantic `Field` 强校验 |
| 同上 → `loginRequest` | 登录不加严 |
| `CreateUser.vue` | `RegisterForm`、`formRef`、`FormRules`、`validate` |
| `Login.vue` | 可有 `LoginForm`；不必套开通那套长度 rules |

**验收：** 开通页密码少于 6 位 → 前端拦住不发请求；用 Postman 故意短密码打 `/register` → 后端 422。

---

## 十五、Agent：查业务库 + 短期记忆 + 结构化输出

> 对照尚硅谷笔记：§10 工具、§12 `create_agent`、§11/Agent 高级 3 结构化、§13 短期记忆。  
> **本项目不上向量库 / RAG**（业务是结构化 MySQL，用 Tool 查库即可）。  
> 代码主文件：`back/agent/agent.py`；入口：`back/router/chat.py`。

## 整条线怎么串起来

```text
用户在「智能助手」发一句
  → POST /api/chat（须登录 get_current_user）
  → set_agent_user(id, role)          # 工具里知道「当前是谁」
  → agent.invoke(messages, config={thread_id: user-N})  # 短期记忆
  → 模型决定调哪个 @tool → SyncSession 查 MySQL → 工具返回文字
  → （可选）收成 AssistantReply.answer
  → extract_reply → 落库 chat_records → 前端展示
```

**一句话：** Agent = 大脑（LLM）+ 手脚（Tools 查你的库）+ 短期记忆（同 thread 续聊）+ 可选结构化最终答案。

---

### 15.1 为什么学 / 边界

| 要解决的问题 | 做法 |
|--------------|------|
| 模型不会查你的等保库，会瞎编 | `@tool` 查 `project` / `gap_item` |
| 每轮 `invoke` 默认不记得上一句 | `InMemorySaver` + 同一 `thread_id` |
| 程序想拿固定字段（如 answer） | `response_format=ToolStrategy(AssistantReply)` |
| 问「等保标准 PDF 怎么写」 | 才需要 RAG → **本项目不做** |

**不做清单（答辩可以说清）：**

- 向量库 / Embedding / Milvus  
- Agent 再套 Redis（业务缓存已在列表/统计；Agent 实时查库）  
- 击穿 / 雪崩 / 分布式锁  
- 长期记忆 Store（聊天历史已在 MySQL，需要时用 `search_chat_history`）  
- 中间件全家桶（Summarization / HITL 等了解即可）

---

### 15.2 改动了哪些文件（复习时对着打开）

| 文件 | 改了什么 |
|------|----------|
| `back/agent/agent.py` | **重写**：四个工具、ContextVar、checkpointer、ToolStrategy、删掉 import 时测试 `invoke` |
| `back/router/chat.py` | 挂登录；`set_agent_user` + `thread_id` 调 Agent；`extract_reply` 后落库 |
| 前端 `Chat.vue` | **未改**（仍调 `/api/chat`，展示 `agent_output`） |

---

### 15.3 模型初始化（课上 §5，你已熟悉）

```python
_env = Path(__file__).resolve().parents[2] / ".env"
load_dotenv(_env, override=True)

model = init_chat_model(
    model=os.getenv("DASHSCOPE_MODEL", "qwen-plus"),
    model_provider="openai",   # 百炼走 OpenAI 兼容驱动
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url=os.getenv("DASHSCOPE_BASE_URL"),
)
```

要点：KEY 在根目录 `.env`；`parents[2]` 从 `back/agent/agent.py` 指到项目根。

---

### 15.4 工具 Tools（课上 §10）——本项目主菜

#### 为什么学

大模型不能直接访问 MySQL。没有工具，只能聊天瞎编。

#### 概念

| 名词 | 含义 |
|------|------|
| `@tool` | 把普通函数变成 LangChain Tool |
| `parse_docstring=True` | 把 docstring / Args 解析给模型看「何时调、参数啥意思」 |
| Tool Calling | 模型产出「调谁、参数」；**你的代码执行**；结果回填后再生成最终回答 |
| `create_agent(..., tools=[...])` | 框架替你跑「模型 ↔ 工具」循环（不必手写 bind_tools 循环） |

#### 本项目四个工具

| 工具名 | 何时调（写在 docstring + system_prompt） | 查什么 |
|--------|------------------------------------------|--------|
| `list_my_projects` | 「我有哪些项目 / 项目列表」 | `project`（经理全库；核查员只成员表） |
| `get_project_gap_stats` | 「待整改有几条 / 状态统计」 | `gap_item` 按 status 聚合 |
| `list_project_gaps` | 「有哪些待整改 / 差距清单」 | `gap_item` 列表，可按 status 过滤 |
| `search_chat_history` | 「以前聊过什么」 | `chat_records`（原 `Test` 表）关键词 |

**权限：** 工具内用 `_require_user()` 取 `(user_id, role)`；非经理查某项目前校验 `project_member`——和第七章数据范围一致。

**为何用 `SyncSessionLocal`：** Agent 工具是**同步**函数，不能 `await`；`config/request.py` 里已用 `pymysql` 同步引擎专门给工具用。

#### 旧 bug（已修）

以前 `get_databases_info` 在 `for` 里 `return`，只会返回第一条。现改名 `search_chat_history`，先拼完 `lines` 再 `return "\n".join(lines)`。

#### 调用链（课上流程图，落到本项目）

```text
用户：「项目1待整改有几条？」
  → AIMessage(tool_calls=[get_project_gap_stats(project_id=1)])
  → 执行工具 → ToolMessage（统计字符串）
  → 再调模型 → 最终 answer（给人看）
```

---

### 15.5 当前用户怎么进工具：ContextVar

课上长期记忆 demo 用 `state["user_id"]` + `ToolRuntime`。本项目用 **`contextvars.ContextVar`**，目的一样：**工具执行时知道当前登录用户**。

```text
路由：token = set_agent_user(current_user.id, current_user.role)
  → 工具：user_id, role = _require_user()
  → finally：reset_agent_user(token)
```

| 函数 | 作用 |
|------|------|
| `set_agent_user` | invoke 前写入 |
| `_require_user` | 工具内读取；没设置就报错 |
| `reset_agent_user` | finally 复位，防串请求 |

**和 thread_id 别混：**

| | 干什么 |
|--|--------|
| ContextVar `(user_id, role)` | 这一次工具查库的**权限身份** |
| `thread_id = user-{id}` | 短期记忆：哪一场对话存档 |

---

### 15.6 短期记忆 Memory（课上 §13）

#### 为什么学

默认每次 `invoke` 无状态；不问「刚才说的项目」会忘。

#### 概念三件套

| 组件 | 本项目 |
|------|--------|
| State | 主要是 `messages`（`ChatAgentState` 还可带 `user_id`） |
| Checkpointer | `InMemorySaver()` —— 进程内存快照 |
| Thread ID | `make_thread_config(user_id)` → `{"configurable": {"thread_id": f"user-{user_id}"}}` |

```python
checkpointer = InMemorySaver()
agent = create_agent(..., checkpointer=checkpointer, ...)

# 路由里：
agent.invoke(
    {"messages": [HumanMessage(...)], "user_id": current_user.id},
    config=make_thread_config(current_user.id),
)
```

#### 必背边界

| 情况 | 结果 |
|------|------|
| 同一 `thread_id` 连续两轮 | 记得上一轮 |
| 换用户（换 thread_id） | 互不串记忆 |
| 后端进程重启 | **InMemorySaver 清空**（答辩够用；生产才换 PostgresSaver） |
| 没挂 checkpointer | 每轮全新，无短期记忆 |

**和 MySQL `chat_records` 别混：**

| | 作用 |
|--|------|
| checkpointer 短期记忆 | 给**模型**看的多轮上下文（进程内） |
| `chat_records` 落库 | 给**前端历史列表**展示；也可用工具 `search_chat_history` 搜 |

---

### 15.7 结构化输出（课上 §11 + Agent 高级 3）

#### 为什么学

普通回答是一段散文；结构化方便程序取字段（本项目取 `answer` 落库/返回前端）。

#### 模型 vs Agent（别混）

| | 模型 `with_structured_output` | Agent `response_format` |
|--|--|--|
| 绑在谁 | 模型对象 | `create_agent` |
| 何时解析 | 每次模型回复 | **任务结束**交最终答案时 |
| 本项目 | 未用 | `ToolStrategy(AssistantReply)` |

```python
class AssistantReply(BaseModel):
    answer: str = Field(description="给用户看的中文回答")
    based_on_tools: bool = Field(description="是否依据了工具结果")

response_format=ToolStrategy(
    schema=AssistantReply,
    tool_message_content="已生成结构化回答",  # 只影响历史里伪 ToolMessage 文案
)
```

**取结果：**

```python
def extract_reply(result: dict) -> str:
    # 优先 structured_response.answer
    # 没有再 messages[-1].content
```

百炼更稳常用 **ToolStrategy**（靠 tool call 收结构）；`ProviderStrategy` 依赖厂商原生能力。

聊天框仍主要展示一段话 → 结构化是「收口字段」，不是另做一套 UI。

---

### 15.8 `create_agent` 一次钉死（课上 §12）

```python
agent = create_agent(
    model=model,
    tools=[list_my_projects, get_project_gap_stats, list_project_gaps, search_chat_history],
    system_prompt="...何时调哪个工具...不要编造...",
    checkpointer=checkpointer,
    state_schema=ChatAgentState,
    response_format=ToolStrategy(schema=AssistantReply, ...),
    name="dengbao_assistant",
)
```

| 参数 | 作用 |
|------|------|
| `model` | 大脑 |
| `tools` | 手脚（2～5 个较合适） |
| `system_prompt` | 角色 + 何时用工具 + 勿瞎编 |
| `checkpointer` | 短期记忆存档 |
| `response_format` | 最终结构化 |
| `name` | 消息归因（可选） |

**已删：** 文件底部 import 就 `agent.invoke(...)` —— 起服务会白打 API。

---

### 15.9 路由怎么接（`chat.py`）

```text
Depends(get_current_user)     → 未登录 401
set_agent_user(...)           → 工具有身份
agent.invoke(..., config=...) → 短期记忆
extract_reply(res)            → 取 answer
post_inset_info(...)          → 写入 chat_records
finally: reset_agent_user     → 清理 ContextVar
```

`/chat/records` 也挂了登录（未登录不能拉历史）。

---

### 15.10 自测三句（登录后打开智能助手）

1. 「我有哪些项目？」→ 应调 `list_my_projects`  
2. 「项目 1 待整改有几条？」（换成你库里真实 id）→ `get_project_gap_stats`  
3. 「刚才说的那个项目叫什么？」→ 测短期记忆（同用户连问）  

核查员账号测：问无权项目 → 工具应返回「无权查看」。

---

### 15.11 答辩口述（1 分钟）

> 大模型无状态、不会查库。我用 LangChain `create_agent`：四个工具查项目列表、差距统计、差距清单和聊天历史；权限跟业务一样按角色/成员过滤。短期记忆用 `InMemorySaver` + 按用户 `thread_id`。最终答案用 Pydantic + `ToolStrategy` 收成 `answer` 字段。业务数据是结构化 MySQL，所以用 Tool Calling，**不上向量库 RAG**；Redis 用在 token 和列表热点读，不塞进 Agent。

---

### 15.12 和课上章节对照（复习导航）

| 课上 | 本项目落点 |
|------|------------|
| §5 模型初始化 | `init_chat_model` + `.env` |
| §8 消息无状态 | 为何要 Memory / 传 messages |
| §10 工具 | 四个 `@tool` |
| §12 `create_agent` | `agent.py` 底部创建 |
| §11 / Agent 结构化 | `AssistantReply` + `ToolStrategy` |
| §13 短期记忆 | `InMemorySaver` + `thread_id` |
| §14 RAG | **刻意跳过** |

---

### 15.13 代码落点速查

| 位置 | 作用 |
|------|------|
| `agent.py` → ContextVar | 当前用户 |
| `agent.py` → 四个 tool | 查库 |
| `agent.py` → `AssistantReply` / ToolStrategy | 结构化 |
| `agent.py` → checkpointer / `make_thread_config` | 短期记忆 |
| `agent.py` → `extract_reply` | 取最终文案 |
| `chat.py` → POST `/chat` | 鉴权 + invoke + 落库 |
| `config/request.py` → `SyncSessionLocal` | 工具同步连库 |

---

## 十六、三四天整体复习地图（临答辩）

> 目标：不求再写新功能，把**已做完的**串成能口述、能演示的一条线。

### 16.1 按天排期

| 天 | 主题 | 读什么 | 动手 |
|----|------|--------|------|
| **D1** | 业务主线 | 本文件 **〇 → 十四**（可跳过过细踩坑，盯「整条线怎么串」） | 登录→项目→差距 action→录入→退出→开通校验走一遍 |
| **D2** | Redis | 根目录 **`缓存点.md`**（先背文首【重点】） | `redis-cli`：登录后看 `auth:token:*`；刷列表看 `project:list:*`；改数据后 key 应被删 |
| **D3** | Agent | 本文件 **十五** + 对着读 `agent.py` / `chat.py` | 智能助手三句自测；重启后端后再问「刚才…」→ 知 InMemory 会丢 |
| **D4** | 串讲 + 收口 | **16.2 口述提纲** + 可选硬化清单 | 计时讲 3～5 分钟；记下卡壳点回看对应章 |

### 16.2 口述提纲（建议顺序）

1. **产品：** 等保整改跟踪——选项目、推进差距状态、留痕迹；经理开通账号/管成员。  
2. **认证：** 登录发 token → 请求带 Bearer → `get_current_user`（可 Redis 先认）→ 角色控按钮与数据范围。  
3. **状态机：** `TRANSITIONS` + `ACTION_ROLES` + 操作日志。  
4. **缓存：** 旁路读；token / 列表 / 统计三块；写后删；不做击穿锁。  
5. **Agent：** 工具查库三句 + 短期记忆 + 结构化；不上 RAG 的理由。  
6. **工程：** 统一响应、401 跳登录、开通前后端校验对齐。

### 16.3 演示最小路径（别翻车）

```text
经理登录
  → 看项目列表（可提一句 Redis）
  → 进差距页改一条状态 + 看统计
  → 智能助手：「我有哪些项目」「某项目待整改几条」「刚才项目叫什么」
  → 退出（若接了删 token 可 redis-cli 验证；未接则说靠 TTL）
核查员登录
  → 只能看见自己的项目（第七章）
```

### 16.4 还知道但可不做（被问到怎么答）

| 项 | 怎么说 |
|----|--------|
| RAG / 向量库 | 问的是库表统计，SQL 工具够；标准文档问答才上 RAG |
| 缓存击穿/锁 | 并发小、有 TTL + 写后删，v1 不做 |
| 退出删 Redis | 函数已有，接口可选补；TTL 兜底 |
| PostgresSaver | 短期记忆生产可换；演示用内存 |

### 16.5 文档索引（别迷路）

| 主题 | 文档 |
|------|------|
| 业务全流程 | `等保实施.md` 〇～十四 |
| Redis | `缓存点.md` |
| Agent | `等保实施.md` **十五** |
| 复习排期 | 本段 **十六** |
