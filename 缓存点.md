# 缓存点（等保项目 · Redis）

> 本文按学习顺序整理：**概念 → 四步落地 → 分层 → 读写模板 → 本项目三块 → 你自己的理解总结 → 进度与自测**。  
> 和新闻课件是同一套思路；本仓库业务是 token / 项目列表 / 状态统计，不是新闻分类。  
> **最重要先看下面「重点口述」**：第四步怎么套策略、key 怎么定、何时 get/set/delete。  
> **整体复习排期**见 `等保实施.md` **十六**；Agent 查库见 **十五**（Agent 不接 Redis）。

---

## 【重点】第四步怎么套——口述总流程（必背）

> 下面是你自己理顺后的说法，已核对可用。答辩/写代码都按这条走。

### 总结构

```text
前三步相对固定：服务端 → 客户端 → 通用 get/set/delete（工具）
第四步最重要：在业务上再套一层策略
  → 策略文件写完后
  → 再回到 router / auth / crud 相应位置调用
     （什么时候读、什么时候存、什么时候删）
```

**一句话：** 策略层定「怎么存」；业务层定「何时读、存、删」。

### 第四步具体怎么套（做一块缓存时的顺序）

1. **先想清要做哪些高频缓存**，再一块块做（本项目：token → 项目列表 → 状态统计）。  
2. **以「项目列表」为例**（套的是统一旁路模板，列表只是其中一个业务）：  
   - 新建 `project_cache.py`（下划线命名，不要 `project.cache.py`）  
   - 把 `cache_conf` 里封装好的方法 **import** 进来  
   - 设置 **TTL**  
   - 用函数拼 **key（键名）**  
3. **key 函数的形参怎么定：**  
   - 看这笔缓存的**作用**  
   - 对照原来 **crud 查库**靠什么条件区分结果  
   - 列表按用户不同 → 形参是 **`user_id`**，不是「项目列表」本身  
   - 列表内容是 **value**；`user_id` 只用来拼钥匙  
4. **key 函数为什么要封装：**  
   - 相当于固定前缀 + 动态部分（如 `project:list:{user_id}`）  
   - 后面的 **get / set / delete 三方都要调用同一个 key 函数**，保证键名一致（模板通用）  
5. **外面这一层（策略文件）写完之后：**  
   - 回到业务里相应位置调用  
   - **读**（要展示）：先 get；没有再查库再 set  
   - **存**（如登录刚造出 token）：写库成功后 set  
   - **删**（数据变了：建项目/加人/删人/改状态）：写库成功后 delete  

### 调用不一定只在 router

| 位置举例 | 可能做什么 |
|----------|------------|
| `loginuser` 登录 | set token |
| `auth.get_current_user` | 先 get，未命中查库再 set |
| `getprojects`（crud 或路由） | 项目列表旁路读 |
| 建项目 / 加人 / 删人 | delete 相关 `project:list:{user_id}` |

### 说法微调（避免口误）

| 容易说岔 | 更准 |
|----------|------|
| 起舰名 | **起键名（key）** |
| 套「项目列表专用另一套模板」 | 套**统一的旁路读/写模板**；列表只是业务之一 |
| key 形参是项目列表 | 形参是 **user_id**；value 才是项目列表 |

### 答辩 30 秒版

> 前三步把 Redis 工具备好。第四步按业务套策略：新建 `xxx_cache`，定 TTL 和 key 函数；key 的参数看「什么不同结果就不同」，对照查库入参。get、set、delete 都走同一个 key 函数。策略写完后，在业务里决定何时读、存、删：读先缓存再查库回填；写先改库再删缓存。

---

## 一、缓存是什么、为什么要有

### 1.1 定义（口述）

缓存 = **临时存放**数据（或计算结果）的地方。下次再用时可以**快速从缓存取**，而不必每次都做耗时的查库。

### 1.2 你的总结（已核对：对）

> 缓存的作用就是：**先从缓存里找；没有再查数据库**。这样可以减轻服务器（尤其是数据库）的压力。

这就是最常用的 **旁路缓存（Cache-Aside）** 读路径：

```text
请求来了要数据
    ↓
① 先查缓存（快）
    ├─ 有（命中）→ 直接返回，不打数据库
    └─ 没有（未命中）
          ↓
        ② 查数据库（慢）
          ↓
        ③ 把结果写入缓存（下次就快了）
          ↓
        ④ 返回给前端
```

**记法：** 有缓存直接回；没有就查库并写进缓存，再回。

### 1.3 为什么常放「高频」数据

| 点 | 说明 |
|----|------|
| 缓存 ≈ 内存 | 比每次打 MySQL 快 |
| 放什么 | **读得多、改得相对少**的热数据 |
| 不适合 | 几乎没人看的冷数据；或改得极频繁、缓存跟不上的数据 |

**你的理解（对）：** 内存快但空间有限 → 优先装经常被问到的。

### 1.4 和「自己存」的关系（易混）

- 不是给模块点个「开启缓存」就自动有。  
- 是**开发者在接口代码里**写：先 get，没有再查库，再 set。  
- 前端只正常发请求，一般不管 Redis。

**未命中顺序不要反：** 先查库得到真数据 → 再写入缓存 → 返回。  
不能「没查库就先 set」。

---

## 二、用什么做缓存 —— Redis

| 点 | 说明 |
|----|------|
| Redis 是什么 | 常驻**内存**的键值库，也常当缓存用 |
| 服务端 | **系统级**安装（如 WSL `apt install redis-server`），**不是**装进 Python venv |
| 客户端 | Python 包 `redis`，装进**项目虚拟环境**（本项目用 `uv pip install redis --python .venv`） |
| 和 MySQL | MySQL 存长久权威数据；Redis 存可丢、可再算的热副本 |

```text
前端 → FastAPI → 先 Redis
                 └─ 没有 → MySQL → 写入 Redis → 返回
```

验证服务端：`redis-cli ping` → `PONG`。

---

## 三、落地四步（固定顺序）

```text
① 安装 Redis 服务端
② 配置 Redis 客户端（Python 连上）
③ 封装缓存操作（get / set / delete）
④ 设计缓存策略并接到业务接口（key、TTL、何时读/写/删）
```

| 步骤 | 白话 | 本项目落点 |
|------|------|------------|
| 1 服务端 | 先有仓库在跑 | WSL `redis-server` |
| 2 客户端 | 项目能连上 | `.venv` 里的 `redis` + `config/cache_conf.py` 连接 |
| 3 封装操作 | 通用读写，不管业务 | `get_str_cache` / `get_json_cache` / `set_*` / `delete_cache` |
| 4 策略 | 缓存什么、key、TTL、接到哪 | `cache/auth_cache.py`、`project_cache.py`… |

**你的概括（对）：** 安装 → 配置连接 → 封装操作 → **用封装好的方法按策略接到业务**。

前三步是工具齐了；第四步才是「这个项目怎么用」。

---

## 四、序列化：Redis 里永远是字符串

### 4.1 你的总结（对，注意 dumps/loads 方向）

> Redis 里存的是序列化后的**字符串**。业务层用 list/dict 时：存要 dumps，取要 loads。

```text
写：list/dict  --json.dumps-->  字符串  --SET-->  Redis
读：Redis  --GET-->  字符串  --json.loads-->  list/dict
```

| 方向 | 函数 | 做什么 |
|------|------|--------|
| 写结构化数据 | `set_json_cache` | 内部 `dumps` 再 set |
| 读结构化数据 | `get_json_cache` | get 后 `loads` |
| 写/读纯文本（如 token→user_id） | `set_str_cache` / `get_str_cache` | 不需要 JSON |

### 4.2 读 JSON 为什么要中间变量 `data`

```python
data = await redis_client.get(key)
if data:
    return json.loads(data)
return None
```

`data` = 刚取出的字符串（或没有时的空）。  
作用：① 先判断有没有缓存；② 再交给 `loads` **反序列化**（不是序列化）。

读字符串常常可以直接 `return await redis_client.get(key)`，不必中间变量。

---

## 五、分层：通用封装 → 策略再包一层 → 路由/认人调用

### 5.1 三层分别干什么

```text
① cache_conf.py     → 通用工具（只认 key / value，不管业务）
② auth_cache / project_cache → 再包一层：定死 key、TTL、存什么
③ router / auth / crud → 在合适位置调用：set / 先 get / delete
```

**你的理解（对）：**  
封装好底层方法后，再封装一层把「业务要用的参数」定进去；然后在 router（或 `get_current_user`）里调用——有时是 **set**，有时是 **先读**。

### 5.2 为什么 key 要写成函数

例如：

```python
def project_list_key(user_id: int) -> str:
    return f"project:list:{user_id}"
```

`get` / `set` / `delete` **都会调用它**，保证三处 key 一模一样。  
路由一般不直接调 key 函数，只调 `get_cached_project_list` 这类。

文件名用下划线：`cache_conf.py`、`project_cache.py`；不要 `cache.conf.py`、`project.cache.py`。

### 5.3 set 一定是「两样」吗

落 Redis 时：**一定是 key + value**（可选再加 TTL `ex`）。  

业务函数参数可以是 2 个、3 个……最后都要收成这一对键值。

```text
set_cached_user_id(token, user_id)
  → key = auth:token:{token}
  → value = str(user_id)
```

---

## 六、第四步的两个固定模板（背这个）

### 模板 A：读接口（旁路读）

```text
① 用约定 key 查 Redis
② 有 → 直接返回
③ 没有 → 查 MySQL（原来的逻辑）
④ set 进 Redis（带 TTL）
⑤ 返回给前端
```

伪代码：

```python
async def 读某某(...):
    cached = await get_xxx_cache(...)
    if cached is not None:
        return cached
    data = await 原来的查库(...)
    await set_xxx_cache(..., data)
    return data
```

### 模板 B：写接口（先改库再删缓存）

```text
① 先改 MySQL（权威数据）
② 再 delete 相关 Redis key
③ 返回成功
```

**为什么删而不是改缓存：** 删最简单。下次读走模板 A，自动查库回填成新数据。  
**若只改库不删缓存：** 库是新的，页面还显示旧的（新闻项目「阅读量不更新」就是这个坑）。

### 登录比较特殊

登录时还没有「先读」——是 **先造 token 写库，再 set 进 Redis**（产生会话，不是旁路读）。

---

## 七、怎么决定 key 的形参（你的设计思路）

**你的思路（对）：**

> 先想清缓存的**作用**是什么 → 对照原来查库函数靠哪些条件区分结果 → 这些条件做成 key 的形参。

```text
① 这笔缓存存的是什么结果？
② 什么不同会导致结果不同？→ 进 key
③ 查库函数的入参，通常就是这些
```

| 缓存作用 | key 示例 | 形参从哪来 |
|----------|----------|------------|
| token → 是谁 | `auth:token:{token}` | 请求里只有 token |
| 某人的项目列表 | `project:list:{user_id}` | 每人看到的列表不同 |
| 某项目状态统计 | `gap:count:{project_id}` | 按项目聚合 |

**key ≠ value：**  
`user_id` 只用来拼钥匙；**返回的是当初 set 进去的整份列表**，不是返回 user_id。

```python
return await get_json_cache(project_list_key(user_id))
#     project_list_key → 只得到钥匙 "project:list:3"
#     get_json_cache   → 用钥匙取出 value（项目列表或 None）
```

**你的理解（对）：** 存的时候用某把钥匙放进去；读的时候还用这把钥匙，就能拿到存进去的值。

---

## 八、本项目计划缓存的三块

| # | 模块 | key | value | 状态 |
|---|------|-----|-------|------|
| 1 | Token 会话 | `auth:token:{token}` | `user_id` 字符串 | ✅ 已接登录 + `get_current_user` |
| 2 | 项目列表 | `project:list:{user_id}` | 项目 list（JSON） | ✅ 已接 `GET /list` + 建项目/加人/删人后 delete |
| 3 | 差距状态统计 | `gap:count:{project_id}` | 各状态数量 dict（JSON） | ✅ 已接 `GET /gap_status_count` + action/录入后 delete |

### 8.1 Token 为何 value 要存 user_id

Redis 是键值对：

| | 存什么 | 作用 |
|--|--------|------|
| key | token | 有没有这张通行证 |
| value | user_id | 这张证是**谁的** |

认人时前端只带 token，必须知道当前用户是谁。只 set 空值不够用。

### 8.2 登录为什么这样写

```python
token = await post_token(db, loginuser.id)       # ① 库里生成并保存 token
await set_cached_user_id(token, loginuser.id)   # ② 把 token→user_id 放进 Redis
```

**你的理解（对，再钉准）：**  
一开始没有 token，要先写库造出来；set 时用的是**刚返回的 token** 和**手里已有的 loginuser.id**，一般**不必再查一遍库**才 set。

参数怎么定：**手里有什么就传什么**——上面两步已经算出 `token` 和 `id`。

### 8.3 认人（get_current_user）流程

```text
剥 Bearer 得到 token
  → get_cached_user_id(token)
      ├─ 有 → 用 user_id 查 User → 返回
      └─ 没有或脏了 → get_user_by_token 查库
                      → 成功则 set 回填 → 返回
```

查库成功后要**回填**，否则冷启动后缓存养不热。

### 8.4 项目列表（已接通）

- 一人一份：`project:list:{user_id}`  
- 读：`router/project.py` → `GET /list`：无搜索时先 get，没有查库再 set；**有 `name` 搜索不走缓存**  
- 写后删：建项目删创建人；加人/删人删被操作用户的 list key  
- TTL：5 分钟（`PROJECT_LIST_TTL`）  
- 策略文件：`back/cache/project_cache.py`  

```text
打开项目列表（无搜索）
  → get_cached_project_list(user_id)
  → 没有 → getprojects 查库 → 转 dict → set → 返回

新建 / 加人 / 删人成功
  → delete_cached_project_list(对应用户 id)
```

### 8.5 差距状态统计（已接通）

- 一项目一份：`gap:count:{project_id}`  
- 读：`GET /gap_status_count` 先 get，没有查库再 set  
- 写后删：`POST /gap/{id}/action`、`POST /gap/create` 成功后 delete  
- TTL：5 分钟；策略文件：`back/cache/gap_cache.py`  

```text
打开差距页拉状态统计
  → get_cached_gap_count(project_id)
  → 没有 → get_gapscount 查库 → set → 返回

状态流转 / 录入差距成功
  → delete_cached_gap_count(project_id)
```

---

## 九、服务端 vs 客户端 vs 虚拟环境（踩过的坑）

| | 服务端 | 客户端库 |
|--|--------|----------|
| 装哪 | 系统（apt） | **项目 `.venv`** |
| 验证 | `redis-cli ping` | `.venv/bin/python -c "import redis"` |

本项目 `.venv` 用 **uv** 建，可能没有 `pip` 命令：

```bash
uv pip install redis --python .venv
```

不要用 `pip3 --user --break-system-packages` 装到用户目录就以为 venv 有了。  
IDE 红线：解释器选到 `back/.venv/bin/python`。

Pylance 报 `get` 返回 `bytes|str|None`：开了 `decode_responses=True` 实际是 str，可用 `cast(str | None, ...)`。

---

## 十、自测清单（token 已通过可对照）

```bash
redis-cli ping
# 登录一次后：
redis-cli keys "auth:token:*"
redis-cli get "auth:token:你的token串"   # 应是用户 id
```

出现 key = 登录写入成功。多条 key = 多次登录换过新 token，旧的等 TTL 或退出时删。

---

## 十一、口述浓缩（答辩 1 分钟）

**优先背文首【重点】口述总流程。** 下面是补强句：

1. 缓存：先 Redis，没有再 MySQL，回填；减轻数据库压力。  
2. Redis 在内存；库是权威，缓存是副本。  
3. 四步：服务端 → 客户端 → 通用封装 → 策略接到接口。  
4. 读：旁路；写：先改库再删缓存。  
5. 本项目：token → 项目列表 → 状态统计（三块都已接）。  
6. 分层：`cache_conf` 通用；`*_cache` 定 key/TTL；业务层定何时 set/get/delete。
7. 课上的击穿/雪崩/分布式锁：本项目并发小、有 TTL + 写后删即可，**v1 不写**。

---

## 十二、相关代码落点

| 文件 | 作用 |
|------|------|
| `config/cache_conf.py` | 连接 + 通用 get/set/delete |
| `cache/auth_cache.py` | token 策略 |
| `router/loginuser.py` | 登录后 set |
| `utils/auth.py` | 认人先 get，未命中查库回填 |
| `cache/project_cache.py` | 项目列表策略 |
| `cache/gap_cache.py` | 差距状态统计策略 |
| `router/project.py` | list / gap_status_count 旁路读；写后 delete |

可选后续：退出接口 `delete_cached_token`；清理误建的 `news_cache.py`、`cache.conf.py`、`project.cache.py`。
